# Non-BRAM Disassembly Remediation Plan

## Purpose

This document tracks the work required to turn the non-BRAM portion of this
repository from a byte-exact, source-backed reconstruction into a consistently
structured and documented disassembly.

Backup RAM programs and data are explicitly out of scope for this plan. They
must not be used when calculating progress or selecting the next milestone.

Byte equality remains a non-negotiable constraint. Semantic recovery must not
change include order, binary layout, regional behavior, or any generated
component unless a separately approved change intentionally does so.

## Reconstruction Boundary

The repository supports byte-exact reconstruction of the Japan, USA, and
Europe 2,048-byte-sector data tracks from tracked source plus explicitly
identified external inputs. It does not independently reproduce a complete
mixed-mode game disc:

- `BADEND.STM`, `GOODEND.STM`, and `PTEST.STM` remain externally supplied
  encoded media streams.
- `ABS.TXT`, `BIB.TXT`, and `CPY.TXT` remain externally supplied ISO
  identification metadata.
- The build emits the retail data-track ISO; it does not build the disc's audio
  tracks or a complete CUE/BIN-style mixed-mode image.

At the initial audit, the existing USA output passed all 133 comparisons in
`REGION=1 ./check.sh`. The repository records successful Japan, USA, and Europe
component comparisons and zero-mismatch data-track comparisons against local
CHDs. A fresh three-region build and CHD comparison was not repeated during the
audit.

## Initial Audit Findings

The audit was performed on commit `407be4b` and excluded `src/Backup RAM/`.
The measurements are heuristics for prioritization, not substitutes for manual
classification.

### What is already strong

- The build and comparison frontends identify the real assembly entry points
  and supported regional variants.
- Newer `src/Level/` modules commonly use descriptive routine names, symbolic
  object fields, functional file boundaries, and useful inline comments.
- Palmtree Panic and Wacky Workbench provide good examples of the intended
  subsystem-oriented level structure.
- Most `.nem`, `.kos`, `.eni`, `.unc`, map, graphics, and audio inputs are
  legitimate assets. A semantic disassembly does not require turning pixels or
  known compressed streams into thousands of `dc.b` declarations.

### Active legacy and hybrid source graphs

- Collision Chaos Acts 1 and 2 select legacy entry graphs directly, although
  partially structured alternatives are present in the tree.
- Tidal Tempest Act 1 also selects legacy entry graphs directly.
- Later Tidal Tempest acts, Quartz Quadrant, Stardust Speedway, and Metallic
  Madness use hybrid graphs that combine descriptive `Level/` modules with
  older `common/`, `objects/`, and `rN/` trees.
- A literal include-graph audit found 95 apparently unselected assembly or
  include files. Some are expected editor data or conditional helpers, but
  others are parallel or unfinished implementations. They require manual
  disposition rather than automatic deletion.

### Naming and commentary debt

The legacy `common/`, `objects/`, and `r1`/`r3`-`r8` trees contain approximately:

- 836 assembly/include files;
- 236,800 lines;
- 997 instruction lines with inline comments; and
- 11,600 address-derived labels before counting `L_FFxxxx` labels.

Across all non-BRAM source, the audit found approximately 17,300 definitions in
the common address-derived families `L_FFxxxx`, `loc_xxx`, `sub_xxx`,
`off_xxx`, and address-named data labels.

Large standalone examples include:

- `src/Time Attack/Main.asm`: 5,210 lines and about 1,600 address-derived
  labels;
- `src/FMV/Main (Ending).asm`: 16,190 lines, about 822 address-derived labels,
  and more than 14,000 `dc.*` declarations; and
- `src/Thank You/Main.asm`: partially named, but still heavily dependent on
  `L_FFxxxx` labels and embedded instruction bytes.

### Unclassified and mechanically emitted data

The non-BRAM assembly/include source contains approximately 149,900 `dc.b`,
`dc.w`, or `dc.l` lines. Data declarations are not inherently debt, but their
meaning must be explicit.

The initial audit found:

- 279 files with names containing terms such as `Padding`, `Legacy`, `Packed`,
  or `Retained`;
- approximately 44,966 `dc.*` lines in those files;
- 175 files that are at least 90 percent declarations and contain at least 100
  declaration lines, including both legitimate mappings and opaque streams;
  and
- 79 tracked binary assets with address-derived names such as
  `byte_238A42_1a.bin`.

The present source-backed status of these bytes is sufficient for
reconstruction, but not for semantic completion.

### Documentation debt

The main documentation is dominated by chronological milestone records. It
does not yet provide a concise architecture guide, active build graph, data
format index, or component-by-component semantic status matrix. The IDA README
is empty.

## Definition of Semantic Completion

A non-BRAM component is semantically complete only when all of the following
are true:

1. Every active executable range is expressed as assembly instructions rather
   than unexplained `dc.*` bytes.
2. Routines have functional names. Temporary address-derived labels are either
   renamed or recorded in a bounded, explicit debt inventory.
3. Important routines document their purpose, inputs, outputs, clobbers, and
   externally visible side effects. Comments should explain intent and unusual
   behavior, not merely restate every instruction.
4. RAM, hardware registers, object fields, commands, flags, and table entries
   use meaningful symbols where their purpose is known.
5. Every data range is classified as a structured table, known compressed
   stream, editable asset, genuine fill/alignment, or explicitly unresolved
   data.
6. Binary assets have descriptive names and documented formats. Binary assets
   may remain binary when that is their natural representation.
7. Each output has one canonical active source graph. Parallel implementations
   are removed, archived as documentation, or explicitly justified.
8. All affected regional component comparisons remain byte-exact.

Repository-wide completion additionally requires all non-BRAM entries in the
status inventory to meet this definition, all three regional checks to pass,
and the regional data tracks to match their reference CHDs when the local
comparison tools and images are available.

## Operating Rules

Every agent working on this plan must follow these rules:

1. Read `AGENTS.md`, this document, the current status inventory, `make.sh` or
   `make.bat`, and the latest relevant commits before editing.
2. Work on one bounded milestone at a time. A milestone should normally cover
   one executable range, one routine family, one data format, or one set of
   outputs that necessarily share the same source.
3. Trace every changed fragment to its true assembly entry points and all
   regional or compile-time variants before editing.
4. Preserve byte output. Prefer symbol aliases, labels, macros, comments, and
   include-boundary changes that do not alter emitted bytes.
5. Run the narrowest meaningful assembly checks during development, then run
   every affected regional comparison required by the shared source graph.
6. Update the status inventory and the progress ledger in this document as
   part of the same milestone.
7. Inspect `git status --short` before committing. Generated files, listings,
   logs, `out/`, proprietary originals, and disc images must remain untracked.
8. Commit each successfully validated milestone separately with a short,
   imperative subject.
9. Push that commit to the current branch's upstream immediately after the
   commit. Verify that the remote branch contains the local commit before
   starting the next milestone.
10. Do not accumulate several completed milestones into one final commit or
    defer all pushes until the end.
11. If validation or push fails, diagnose it before starting another
    milestone. Do not mark the milestone complete until its required checks
    pass and its commit is present on the remote.
12. Never weaken the displayless build safety rules to make an assembler run.
    Report a missing prerequisite or known headless tool limitation instead.

Commit subjects should describe the semantic result, for example:

- `Migrate Collision Chaos Act 1 entry graph`
- `Name Time Attack VDP initialization routines`
- `Classify R31B packed mapping stream`
- `Document legacy level data formats`

## Progress Tracking

Create `docs/disassembly-status.md` during milestone M1. It should contain one
row per built non-BRAM component, with at least these fields:

- output name;
- assembly entry point;
- active source graph (`structured`, `hybrid`, or `legacy`);
- code status;
- data status;
- unresolved labels/ranges;
- binary asset status;
- affected regions and switches;
- last validation result; and
- next recommended milestone.

Machine-generated measurements should live in a repeatable audit script rather
than being manually recomputed in prose. The script must exclude BRAM and
distinguish active source from tracked but unreachable source. Its output is an
aid to manual review; it must not automatically classify declarations as
opaque merely because they use `dc.*`.

### Milestone ledger

| Milestone | Status | Scope | Validation and handoff |
| --- | --- | --- | --- |
| M0 | Complete | Establish the non-BRAM audit, definitions, roadmap, and per-milestone commit/push protocol. | Existing USA output: all 133 `check.sh` comparisons matched; documentation-only change. |
| M1 | Complete | Add the 124-component status matrix and repeatable non-BRAM audit tooling. | Audit/check and JSON invariants pass: all build/check/status sets agree, active plus unreachable equals tracked, zero active includes are missing, and repository hygiene is clean. Documentation-only; no fresh assembly was required. |
| M2 | Complete | Normalize the shared Level Sonic animation include to its exact filesystem case. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including the eleven affected Palmtree Panic retail/demo outputs. |
| M3 | Complete | Converge the shared Collision Chaos present-time palette-cycle routine used by R31A and R32A. Replace the active address-labelled implementation with the descriptive source, preserve the original fall-through and short-call encodings, correct the first cycle color to `$0EE`, and remove the superseded duplicate. | Audit/check reports zero missing or case-mismatched includes and one fewer unreachable source. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A__.MMD` and `R32A__.MMD`. |
| M4 | Complete | Converge the shared `LoadPalette` implementation across all ten Collision Chaos outputs. Route the legacy and hybrid R3 graphs through the descriptive shared routine after confirming its label, ABI, instructions, and layout match; leave the four differently named palette fade/load routines for a dedicated compatibility milestone. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M5 | Complete | Converge the shared fade-palette loader across all ten Collision Chaos outputs. Give the descriptive `LoadFadePal` routine an opt-in `LoadFadePalette` entry alias for recovered graphs, then route every legacy and hybrid R3 graph through that canonical implementation. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M6 | Complete | Converge the shared water and water-fade palette loaders across all ten Collision Chaos outputs. Give `LoadWaterPal` and `LoadWaterFadePal` opt-in recovered-ABI entry labels, then route every legacy and hybrid R3 graph through the canonical descriptive implementations. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M7 | Complete | Converge the complete palette-fading engine across all ten Collision Chaos outputs. Bind its newer `ProcessPLCs` service name to the recovered byte-identical `AdvanceGfxQueue` implementation in the R3 compatibility layer, then replace every historical palette-fade include with the descriptive shared source. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M8 | Complete | Converge Collision Chaos Present palette data shared by R31A and R32A. Model the two distinct full stage palettes with a named record macro, retain semantic and recovered aliases for their consumers, route both outputs through the descriptive table/assets, and remove the superseded address-labelled source. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked source, and two fewer unreachable ASM/INC files. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A__.MMD` and `R32A__.MMD`. |
| M9 | Complete | Converge Collision Chaos Past palette data shared by R31B and R32B. Correct the descriptive table to its single stage-palette entry, retain the recovered stage-palette alias, activate the variant source with `CC_VARIANT=1`, and remove the superseded address-labelled source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31B__.MMD` and `R32B__.MMD`. Europe was rerun successfully after disposable prior validation outputs exhausted local disk space before assembly. |
| M10 | Complete | Converge Collision Chaos Good Future palette data shared by R31C, R32C, and R33C. Correct the pointer-table order, model the live and level-end stage palettes with a named record macro, retain recovered aliases for all four palette records, activate the source with `CC_VARIANT=2`, and remove the superseded address-labelled source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. After correcting a column-sensitive `EQU` syntax error found by the first build, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31C__.MMD`, `R32C__.MMD`, and `R33C__.MMD`. |
| M11 | Complete | Converge Collision Chaos Bad Future palette data shared by R31D, R32D, and R33D. Add recovered aliases to the already correctly ordered descriptive records, activate the source with `CC_VARIANT=3`, and remove the final address-labelled time-period palette source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31D__.MMD`, `R32D__.MMD`, and `R33D__.MMD`. |
| M12 | Complete | Converge the no-op Collision Chaos Past palette cycle shared by R31B and R32B. Add the recovered `CyclePalette` entry alias to the descriptive routine, route both graphs through the canonical variant source, and remove the superseded six-line source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31B__.MMD` and `R32B__.MMD`. |
| M13 | Complete | Converge the Good and Bad Future palette-cycle engine shared by R31C-D, R32C-D, and R33C-D. Preserve short intermediate calls and final-cycle fall-through in the descriptive variant routine, move the Act 3 variant selection ahead of the shared cycle/data includes, and remove the final two address-labelled palette-cycle sources. | Audit/check reports zero missing or case-mismatched includes and two fewer tracked sources. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31C-D`, `R32C-D`, and `R33C-D` outputs. The complete Collision Chaos palette subsystem now has one canonical source graph. |
| M14 | Complete | Converge the shared Sonic 1 despawn, VSync, sine/cosine, and angle-calculation cluster across all ten Collision Chaos outputs. Add opt-in recovered entry aliases to `Level/Functions (Misc).asm` and replace the three historical includes in each R3 graph; retain the historical files because other zone graphs still consume them. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M15 | Complete | Converge the shared player floor-collision and block-collision engine across all ten Collision Chaos outputs. Route every R3 graph through `Level/Collision Floor.asm`, expose the recovered public entry names and `StageChunks` binding only for semantic R3 consumers, and retain the two historical common files because Tidal Tempest, Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness still consume them. | Audit/check reports zero missing or case-mismatched includes. After validation exposed and bounded the missing `LevelChunks` binding and the Tidal Tempest compatibility scope, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M16 | Complete | Converge the ten-file general-service sequence across all ten Collision Chaos outputs. Replace recovered random, controller, VDP, Z80, FM queue, tilemap, Nemesis, PLC, Enigma, and Kosinski fragments with `Level/Functions (General).asm`; bind historical public names only for R3 consumers, retain the recovered no-op `PlayFmMusic` label on the semantic initializer's existing unreachable `rts`, and omit the duplicate player-selection helper supplied by R3 scrolling. Retain the historical common files because other zone graphs still consume them. | Audit/check reports zero missing or case-mismatched includes. Validation first bounded three compatibility namespace collisions and then identified a repeated two-byte growth caused by emitting rather than labelling the existing no-op instruction. After those corrections, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe; all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M17 | Complete | Converge the shared level main loop and interrupt handlers across all ten Collision Chaos outputs. Route every R3 graph through `Level/Main.asm`, suppress the recovered-to-semantic aliases inherited from the R6 layer for these consumers, and bind the structured main loop to the recovered R3 scroll, draw, loading, object, Sub CPU, animation, HUD, collision, and graphics providers without changing their instruction streams. Retain `common/main.asm` because Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness still consume it. | Audit/check reports zero missing or case-mismatched includes. The first Japan build identified the complete inverse-ABI surface as unresolved symbols; after binding those definitions, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A-D`, `R32A-D`, and `R33C-D` outputs. |
| M18 | Complete | Converge Collision Chaos Act 1 Present scrolling for `R31A`. Route the recovered graph through `Level/Collision Chaos/Scroll (Act 1 Present).asm`, preserve the retained Sonic 1 ending/demo start-position path, selected-player `a6` ABI, and USA event-before-camera ordering behind an R3-scoped compatibility switch, and remove the superseded recovered source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. Validation bounded a 42-byte omitted demo-start path, direct-player addressing differences, and the USA-specific event order; after restoring those retained behaviours, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M19 | Complete | Converge Collision Chaos Act 1 Present stage drawing for `R31A`. Replace the recovered composite with `Stage Draw (Act 1 Present).asm` and the shared `Block Draw.asm`, preserve the historical placement of the block engine between live drawing and initialization under an R3-scoped switch, bind the recovered `PlaceBlock` ABI to `PlaceBlockAtPos`, and remove the superseded recovered source. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. The first USA build exposed the missing block-replacement ABI; the next comparison localized address differences to the composite module's observable include order. After preserving both details, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M20 | Complete | Converge Collision Chaos Act 1 Present level-data loading for `R31A`. Route the recovered graph through `Palmtree Panic/Load Level Data.asm`, expose its recovered `LoadStageData`, `InitSectionGfx`, and `StageMaps` providers through R3-scoped semantic aliases, and remove the superseded recovered loader. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. The first USA build showed that the deliberately reduced R31A compatibility surface also needed an explicit `LevelLayouts` binding; after adding it, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M21 | Complete | Converge the shared level-event engine for `R31A`. Route the graph through `Level/_Events.asm`, let the semantic `RunLevelEvents` entry own the implementation, and retain an R3-scoped inverse `StageEvents` alias for recovered callers. Retain `common/events.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`; the non-USA runs also verify the region-conditional Quartz Quadrant event bytes emitted within the shared module. |
| M22 | Complete | Converge the shared object update, movement, drawing, sprite, and visibility services for `R31A`. Route the graph through `Level/Object Functions.asm` and bind its semantic entries to the recovered `UpdateObjects`, `MoveObjectFall`, `MoveObject`, `DeleteOtherObject`, and visibility ABI under an R3-scoped switch. Retain `common/object.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M23 | Complete | Converge the `R31A` object dispatch table. Route the graph through `Collision Chaos/Object Index (Act 1 Present).asm`, bind its descriptive object entries to the recovered providers under an R3-scoped switch, use the table's colocated semantic null routine, and remove the superseded R31A table. Retain `objects/null.asm` because sixteen other legacy graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and one fewer tracked source. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, verifying every absolute object pointer and downstream address in affected `R31A__.MMD`. |
| M24 | Complete | Converge the Sub CPU command service for `R31A`. Route the graph through `Level/Sub CPU.asm`, invert the recovered `SubCpuCommand`/semantic `SubCPUCmd` ABI under an R3-scoped switch, and retain `common/sub_cpu.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M25 | Complete | Converge the shared object-animation interpreter for `R31A`. Route the graph through `Level/Object Animate.asm`; the semantic source preserves all six script-control opcodes and the recovered offset-1 animation-frame access without requiring compatibility aliases. Retain `common/object_animate.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M26 | Complete | Converge the checkpoint object and checkpoint-save routine for `R31A`. Route the graph through `Level/_Objects/Checkpoint/Main.asm`, bind the recovered object, mapping, animation, allocation, and despawn providers under an R3-scoped switch, and preserve R31A's saved mini-Sonic byte. Retain `objects/checkpoint.asm` because sixteen other legacy graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. The first Japan build identified the two later recovered service providers needed by the semantic object; after binding them, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M27 | Complete | Converge the Sonic 1 drowning-music reset leftover for `R31A`. Route the graph through the documented `Level/Resume Music (Leftover).asm` no-op and bind recovered `PlayerResetDrown` calls to semantic `ResumeMusicS1` under an R3-scoped switch. Retain `common/drown_reset.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M28 | Complete | Converge the unused test badnik for `R31A`. Route the graph through `Level/_Objects/Test Badnik/Main.asm` and bind the recovered dispatch, powerup mappings, and powerup animation providers under an R3-scoped switch. Retain `objects/test.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M29 | Complete | Converge the explosion and points-creation object for `R31A`. Route the graph through `Level/_Objects/Explosion/Main.asm`, bind the recovered dispatch and asset providers, and document/expose the byte-neutral `SpawnPoints` entry used directly by the R3 bumper, glass-shatter, and points-pocket objects. Retain `objects/explosion.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. The first Japan assembly showed that the recovered global entry changed local-label scope; promoting the end label to `ObjExplosion_MakePoints_End` preserved the structure and bytes. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M30 | Complete | Converge the flower seed/growth object for `R31A`. Route the graph through `Level/_Objects/Flower/Main.asm`, bind its recovered dispatch, mappings, animation, and floor-distance providers, preserve the non-water tile-priority path, and retain the semantic classification of its unfinished respawn bookkeeping. Retain `objects/flower.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M31 | Complete | Converge the spin-tunnel waterfall splash object for `R31A`. Route the graph through `Level/_Objects/Spin Tunnel/Main (Waterfall Splash).asm` and bind its recovered dispatch, mapping, and animation providers under an R3-scoped switch while preserving the Past-specific tile selection. Retain `objects/r1/tunnel_splash.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M32 | Complete | Converge the spin-tunnel door, door-splash setup, and door-splash objects for `R31A`. Route the graph through `Level/_Objects/Spin Tunnel/Main (Door).asm`; bind its recovered dispatch, mappings, animations, and tile-ID service under an R3-scoped switch; and preserve both the retail narrow-door subtype path and the scrapped splash-setup entry. Retain `objects/r1/h_door.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M33 | Complete | Converge the shield, invincibility-star, and time-warp-star powerup object and its animated-art loader for `R31A`. Route the graph through `Level/_Objects/Powerup/Main.asm` and bind its recovered dispatch, art-loader entry, mappings, animations, and five art providers under an R3-scoped switch while preserving Collision Chaos's layer-priority path. Retain `objects/powerup.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. The first Japan assembly exposed the recovered animated-tile caller and art-provider ABI that is normally hidden by the shared semantic-player compatibility branch; after binding those names explicitly, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M34 | Complete | Converge the Collision Chaos roll-tunnel object for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Roll Tunnel/Main.asm`, bind its recovered dispatch under an R3-scoped switch, and explicitly preserve R31A's all-region X-radius write and low-byte debug-mode test through a graph-local legacy ABI selector. Retain `objects/roll_tunnel.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`; the Japan and Europe results verify the two encodings selected by the R31A-specific ABI path. |
| M35 | Complete | Converge the boulder object for `R31A`. Route the graph through `Level/_Objects/Boulder/Main.asm`, bind its recovered dispatch under an R3-scoped switch, and finish the semantic mapping data by replacing its address-derived frame label and unexplained literal tail byte with a named local frame and the containing module's ordinary alignment. Retain `objects/r1/boulder.asm` because the other legacy zone graphs still consume it. | Audit/check reports zero missing or case-mismatched includes, and the semantic boulder mapping has no residual address-derived labels. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M36 | Complete | Converge the floating-block object for `R31A`. Route the graph through `Level/_Objects/Floating Block/Main.asm`, bind its recovered dispatch under an R3-scoped switch, and finish its semantic data by replacing three address-derived animation labels and six address-derived mapping labels with named scripts and sprite frames, compact frame records, and explicit alignment. Retain `objects/r1/float_block.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes, and the semantic floating-block data has no residual address-derived labels. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M37 | Complete | Converge the normal, attached-sprite, and moving-spring code graph for `R31A`. Route the graph through `Level/_Objects/Spring/Main.asm` and bind both recovered dispatch entries, the four recovered mapping names used by later debug data, and the recovered bottom-solidity service under an R3-scoped switch. Retain `objects/spring.asm` because the other legacy zone graphs still consume it. The canonical spring animation and mapping files remain explicitly unresolved because their address-derived data labels require a separate structural milestone. | Audit/check reports zero missing or case-mismatched includes. The first Japan assembly exposed the later recovered `BottomSolidObject` provider required by the semantic downward spring; after binding it, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M38 | Complete | Structure the six canonical spring animation and mapping files shared by `R31A` and the other semantic spring graphs. Replace all address-derived script/frame labels with descriptive names, consolidate fragmented bytes into animation commands and five-byte sprite pieces, and express every required padding byte as explicit record alignment. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical spring data. The first Japan assembly showed that the vertical table's forward local labels crossed the horizontal table's public-label scope; promoting all nine shared mapping frames to descriptive global labels resolved the source structure. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M39 | Complete | Converge the placed-ring, lost-ring, collection, and sparkle code graph for `R31A`. Route the graph through `Level/_Objects/Ring/Main.asm` and bind both recovered dispatch entries, the recovered mapping/animation names used by later objects, and the recovered allocate-after-current service under an R3-scoped switch. Retain `objects/ring.asm` because the other legacy zone graphs still consume it. The four canonical ring data files remain explicitly unresolved because their address-derived records require a separate structural milestone. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M40 | Complete | Structure the four canonical ring data files shared by `R31A` and the other semantic ring graphs. Replace the address-derived sparkle-script and sprite-frame labels with descriptive names, consolidate fragmented bytes into animation commands and five-byte sprite pieces, and let the containing module's existing alignment represent the normal ring table's genuine tail padding. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical ring data. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, verifying the nine normal-ring frames, four Sonic 1 large-ring views, and eight large-ring flash frames. |
| M41 | Complete | Converge the time icon, time post, monitor shell, and spawned monitor-item code graph for `R31A`. Route the graph through `Level/_Objects/Monitor and Time Post/Main.asm` and bind both recovered dispatch entries plus the recovered mapping/animation names used by later debug data under an R3-scoped switch. Retain `objects/monitor_time.asm` because the other legacy zone graphs still consume it. The two canonical monitor data files remain explicitly unresolved because their address-derived records require a separate structural milestone. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the region-specific invincibility and speed-shoes timer paths in affected `R31A__.MMD`. |
| M42 | Complete | Structure the two canonical monitor/time-post data files shared by `R31A` and the other semantic monitor graphs. Replace all address-derived script and frame labels with descriptive reward, time-post, shell, and HUD roles; consolidate fragmented bytes into animation commands and five-byte sprite pieces; and express every required internal padding byte as record alignment. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical monitor/time-post data. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, verifying ten animation scripts and twenty mapping frames. |
| M43 | Complete | Converge the points popup, HUD object, score/ring update, and numeric drawing code graph for `R31A`. Route the graph through `Level/_Objects/HUD and Points/Main.asm`, invert the recovered main-loop HUD update bridge, and bind the recovered combined-object dispatch and HUD-number art provider under an R3-scoped switch. Retain `objects/hud_points.asm` because the other legacy zone graphs still consume it. The two canonical HUD/points mapping files remain explicitly unresolved because their address-derived records require a separate structural milestone. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M44 | Complete | Structure the two canonical HUD/points mapping files shared by `R31A` and the other semantic HUD graphs. Replace all ten address-derived frame labels and their twenty definitions/references with named reward-tier, score, lives, debug-position, and rings roles; consolidate fragmented bytes into five-byte sprite pieces; and express required internal padding as record alignment. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical HUD/points data. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, verifying six points-popup frames and four HUD frames. |
| M45 | Complete | Converge the Collision Chaos bumper and flipper code graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Bumper and Flipper/Main.asm`, bind the recovered dispatch and mapping providers, and preserve the historical `d1` bumper dispatcher, `ADDI #-1` step counter, absolute asset/slope addressing, shared `SpawnPoints` call, and recovered despawn provider behind an R31A-only compatibility switch. Retain `objects/r3/bumper_flipper.asm` because the other legacy R3 and R8 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Validation first exposed the recovered despawn-provider ABI and then localized the only byte difference to the bumper dispatcher's destination register. After bounding both details, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M46 | Complete | Converge the Collision Chaos moving bumper-platform code graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Bumper Platform/Main.asm`, bind the recovered dispatch and mapping providers used by the object index and later debug data, and preserve the historical `ADDI #-1` movement-step encoding behind an R31A-only compatibility switch. Retain `objects/r3/bumper_platform.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M47 | Complete | Converge the Collision Chaos glass-shatter code graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Glass Shatter/Main.asm`, bind the recovered dispatch, animation, and mapping providers, and preserve the recovered graph's direct call to the earlier shared `SpawnPoints` entry without emitting the semantic helper body behind an R31A-only compatibility switch. Retain `objects/r3/glass_shatter.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and all downstream absolute addresses. |
| M48 | Complete | Converge the Collision Chaos Ga parent and dust-child code/data graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Ga/Main.asm`, bind the recovered dispatch, time-check, animation, and mapping providers, and preserve the recovered entry's uncleared `d0`, five `ADDI #-1` timer encodings, absolute parent-animation reference, backward reuse of the first animation tail, and animation/mapping/dust include order behind an R31A-only compatibility switch. Retain `objects/r3/ga.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Validation first exposed the recovered `DestroyInGoodFuture` provider bridge, then the ISO size check localized a two-byte growth to the semantic entry's extra `moveq`. After bounding both details, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M49 | Complete | Converge the unused Collision Chaos teleporter object for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Teleporter/Main.asm` and bind the recovered dispatch and mapping providers used by the object index and later debug data. The canonical source documents the retail implementation as an immediate deletion stub while retaining its classified sprite mapping. Retain `objects/r3/teleporter.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M50 | Complete | Converge the Collision Chaos points-pocket parent, player-capture checker, and animated child graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Pocket/Main.asm`, bind the recovered dispatch, animation, and mapping providers, and preserve the recovered word-branch topology, checker-before-caught continuation, nine `ADDI #-1` timers, `a6` player-control update, recovered player-width field, `ADDI #-$10` Y adjustment, two shared `SpawnPoints` tail calls without a private helper body, absolute player-control bit clears, and child parent-check branch behind an R31A-only compatibility switch. Retain `objects/r3/points_pocket.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Validation first localized a two-byte shrink and checker displacement to the caught-continuation order, then identified the recovered player-width/Y-adjust encodings and final word-sized child branch. After bounding those details, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M51 | Complete | Converge the Collision Chaos Tentou parent and bomb-child code/data graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Tentou/Main.asm`, bind the recovered dispatch, collision-distance, animation, and mapping providers, and preserve the recovered uncleared subtype register, `ADDI #-1` subtype/timer encodings, indirect left/right wall-check dispatch, animation/mapping order, and two bomb timers behind an R31A-only compatibility switch. Restore and classify the retained unreferenced player-range helper as executable code with a functional name and result description rather than omitting or emitting it as unexplained data. Retain `objects/r3/tentou.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and the restored unused helper range. |
| M52 | Complete | Converge the Collision Chaos rotating spike-chain parent and three Present-stage child segments for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Spike Chain/Main.asm`, bind the recovered dispatch and two mapping providers used by the object index and later debug data, and preserve the recovered subtype-load flag encoding behind an R31A-only compatibility switch. The canonical source explicitly documents that R31A's recovered `STAGE_TIME=1` topology emits three visible segments. Retain `objects/r3/spike_chain.asm` because the other legacy R3 graphs and the distinct R6 implementation still consume their historical sources. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M53 | Complete | Converge the `R31A` section-art loader, update routine, range/PLC tables, and fixed object-tile helper. Replace the recovered composite with `Level/Section Art Load.asm` plus `Level/Collision Chaos/Section Data (Act 1 Present).asm`, make the semantic load/update/tile entries canonical, retain byte-neutral recovered routine/table aliases, and document the `$4F2` tile helper's boulder/tunnel-door/spring role. Retain `r3/section_gfx_1a.asm` because the other legacy R3 graph families still use their historical section modules. | Audit/check reports zero missing or case-mismatched includes. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and its loader→tables→tile-helper layout. |
| M54 | Complete | Recover the Collision Chaos stationary, attached, and four-direction retracting spikes implementation into the new canonical `Level/Collision Chaos/Objects/Spikes/Main.asm` module for `R31A`. Replace all address-derived routine/data labels with functional names, document object-state fields and the four width/radius/collision subtype records, express every active executable byte as an instruction, retain the R3 custom-despawn contract, and use the already structured four-frame R3 mapping asset. Retain `objects/r3/spikes.asm` because the other legacy R3 graphs still consume it; do not substitute the behaviorally narrower shared Palmtree spikes object. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the new canonical module or its mapping asset. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M55 | Complete | Converge the Collision Chaos moving block and player-responsive hidden block pair for `R31A`. Route both recovered implementations through the combined canonical `Level/Collision Chaos/Objects/Block/Main.asm`, bind their recovered dispatch and mapping providers, and preserve the historical custom R3 despawn checks, short solid-object call, byte-sized motion-table indexing, stationary early-return placement, PC-relative hidden-block helper calls, shared clamp-return targets, and intervening block-mapping layout behind an R31A-only compatibility switch. Retain `objects/r3/block.asm` and `objects/r3/hidden_block.asm` because the other legacy R3 graphs still consume them. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical block module or its two mapping assets. Validation localized the initial differences to the moving block's early-return position and word-sized table indexing, then to the hidden block's shared clamp-return branches. After preserving those bounded encodings, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M56 | Complete | Converge the Collision Chaos metal-platform dispatcher, player-weight bobbing, four looping horizontal/vertical motion paths, and three delayed-drop paths for `R31A`. Route the recovered implementation through `Level/Collision Chaos/Objects/Metal Platform/Main.asm`, bind its recovered dispatch and mapping providers, document the subtype families and four-byte motion-record schema, and preserve the historical custom R3 despawn flow, byte-sized table indexing, absolute motion-table addresses, PC-relative helper calls, setup-frame returns, shared solid/return joins, and later drum-platform player-slot helper dependency behind an R31A-only compatibility switch. Retain `objects/r3/metal_platform.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical metal-platform module or its mapping asset. Validation first exposed the recovered solid helper's narrower ABI and then localized all remaining size differences to duplicated return sites in the semantic control-flow layout; modelling those as shared named joins restored the exact stream without opaque data. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M57 | Complete | Converge the Collision Chaos vertically triggered sliding door for `R31A`. Route the recovered implementation through `Level/Collision Chaos/Objects/Door/Main.asm`, bind its recovered dispatch and mapping providers, document its subtype sign/delay roles, and preserve the custom R3 despawn check plus the historical direction-first control flow, separate PC-relative offset updater, and later drum-platform player-slot helper dependency behind an R31A-only compatibility switch. The canonical four-byte delay table and two-piece mapping frame remain structured data. Retain `objects/r3/door.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical door module. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`; the same builds also verified the ordinary semantic door path used by the newer graph. |
| M58 | Complete | Converge the complete Collision Chaos fire-shooter family for `R31A`: subtype-zero emitter, subtype-one five-state flame, and both subtype-two accelerating projectiles. Route the graph through `Level/Collision Chaos/Objects/Fire Shooter/Main.asm`, bind the recovered dispatch/animation/mapping providers, document all three subtype roles and the animation/mapping records, and preserve the two custom R3 despawn paths, uncleared initial subtype register, emitter/flame initializer returns, relocated short solid helper, later drum-platform player-slot call, short lowering branch, byte-sized projectile Y adjustment, word-sized flame-timer clear, and recovered projectile deletion topology behind an R31A-only compatibility switch. Retain `objects/r3/fire_shooter.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical family or its animation/mapping data. Validation localized one short-call range failure to the recovered solid helper's historical placement, two size differences to the uncleared subtype register and initializer returns, and the final opcode difference to the word-sized timer clear. After modelling each as executable structure, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M59 | Complete | Converge the Collision Chaos drum-platform dispatcher, subtype-selected bounce states, solidity adjustment, motion records, priority table, and mapping graph for `R31A`. Route the graph through `Level/Collision Chaos/Objects/Drum Platform/Main.asm`, bind its recovered dispatch and mapping providers, document the subtype/state families and three-byte motion-record schema, and preserve the custom R3 despawn flow plus the historically placed player-slot helper consumed by the preceding metal-platform, door, and fire-shooter modules behind an R31A-only compatibility switch. Give that cross-object helper a functional canonical name without moving its executable bytes. Retain `objects/r3/drum_platform.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical drum-platform module or its mapping asset. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and the three earlier objects' PC-relative helper calls. |
| M60 | Complete | Recover the Collision Chaos saved-object despawn helper into the new canonical `Level/Collision Chaos/Object Despawn.asm` module for `R31A`. Document the three-byte saved-object record, time-period selection, loaded-bit contract, and no-record deletion path; express the whole routine with semantic object/RAM names and a functional local label; and preserve its exact location after the drum-platform graph because eight canonical obstacle paths branch to it. Retain `r3/object_despawn.asm` because every other legacy R3 graph still consumes it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels or unexplained data declarations in the canonical helper. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and its eight incoming despawn branches. |
| M61 | Complete | Converge the Collision Chaos KamaKama parent and thrown-sickle child family for `R31A`. Route the graph through `Level/Collision Chaos/Objects/KamaKama/Main.asm`; name its parent timer and the sickle's parent, launch-delay, parent-flags, base-X, gravity, and damage-timer fields; document positive/negative subtype behavior and parent-slot lifetime validation; give both animation tables functional script names; and document the five-byte mapping-piece schema. Preserve the recovered shared animation join, PC-relative movement/spawn calls, parent-to-turnaround fall-through, negative-subtype early return, explicit parent-ID displacement, animation/mapping placement, and later parent-map providers behind an R31A-only compatibility switch. Retain `objects/r3/kama_kama.asm` because the other legacy R3 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical family or its animation/mapping assets. The first Japan build exposed two recovered collision-service names; the first comparison then localized its only four differing bytes to the negative-subtype sickle-spawn early return. After modelling both contracts explicitly, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M62 | Complete | Converge the shared flower-capsule, big-ring/flash, goal-post, signpost, results-trigger, and capsule-palette level-end family for `R31A`. Route the recovered graph through `Level/_Objects/Level End/Main.asm`, bind its semantic object entries and later R3 animation/mapping providers, and expose the recovered entries and big-ring-flash asset names for downstream consumers behind an R31A-only compatibility switch. Documented canonical routines retain structured capsule explosion offsets, seed velocities, goal-post tile IDs, time-bonus records, palette data, animation/mapping data, and Nemesis art; retain `objects/stage_end.asm` because other legacy R3/R4/R5/R6/R7/R8 graphs still consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical family. The first Japan build identified the recovered graph's unlabeled `StageDataIndex+$E` palette byte as the semantic `LevelPaletteID`; after adding that byte-neutral binding, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD`. |
| M63 | Complete | Recover the active Collision Chaos introductory Metal Sonic parent range for `R31A`. Replace its address-derived dispatcher, six state labels, control-flow joins, bobbing helper, exhaust spawner, capture/abort path, and shared movement-helper names with functional labels; document its object-state ABI, side effects, fixed-point motion, and child-link contract; and propagate semantic names for the interleaved composite's shared facing, animation, palette, and player-load helpers without moving any executable bytes. Keep Amy, spikes, hearts, and exhaust as explicit follow-up ranges because their historical interleaving makes the complete composite too broad for one reviewable milestone. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by nineteen, from 16,519 to 16,500. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and downstream absolute addresses. |
| M64 | Complete | Recover the active Collision Chaos Amy Rose parent, shared cutscene-animation engine, palette loader/table, heart emitter, and heart-child ranges for `R31A`. Replace their address-derived dispatchers, lifecycle states, acceleration/clamp joins, animation-parser states, palette records, spawn exits, and heart timing phases with functional labels; document Amy's object-state ABI and side effects, the two-byte animation-frame record, palette fall-through contract, overflow-driven heart emission, and heart lifetime; and classify the retained unreachable return between Amy's initialization and running states as an explicit executable leftover rather than unexplained data. Preserve the composite's historical interleaving and leave intro spikes and Metal Sonic exhaust as bounded follow-up ranges. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by twenty-seven, from 16,500 to 16,473. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and downstream absolute addresses. |
| M65 | Complete | Recover the active Collision Chaos introductory spike-barrier parent and three flying shard children for `R31A`. Replace the address-derived dispatcher, solid/hurt flow, Metal Sonic slot scan and proximity joins, subtype setup, shard-spawn loop, motion initializer/table, lifetime state, and delete entry with functional labels; document all four subtype roles, the three-shard child contract, four-byte X/Y velocity records, gravity/lifetime fields, and the historical sound-to-motion fall-through that also initializes the broken parent. Preserve every active byte as structured instructions or classified motion records while leaving the following Metal Sonic exhaust range for a separate milestone. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered spike range, and reduces active address-derived labels by fifteen, from 16,473 to 16,458. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and downstream absolute addresses. |
| M66 | Complete | Recover the final active Collision Chaos introductory-object executable range for `R31A`: Metal Sonic's exhaust child and its animation script. Replace the address-derived dispatcher, initialization/main states, position join, and deletion entry with functional labels; document the 16-bit parent-slot link, object-ID lifetime check, facing-dependent follow offset, and four-frame looping flicker record; and give the single animation table/script descriptive local labels. Together with M63-M65, the complete interleaved `objects/r3/intro.asm` executable composite now has no address-derived label definitions while retaining its historical layout. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels anywhere in the active intro composite, and reduces active address-derived labels by three, from 16,458 to 16,455. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including affected `R31A__.MMD` and downstream absolute addresses. |
| M67 | Complete | Recover the shared Collision Chaos freed-animal and Metal Sonic projector-hologram family used by all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Replace both address-derived dispatchers, flying/ground lifecycle states, orbit helper, shared draw/delete/flip/tile helpers, and base-tile table label with functional names; document the subtype split, projector parent-slot contract, flying orbit fields, 16.16 ground velocity/gravity behavior, landing-state progression, and `[act * 4 + time_zone]` art selection. Structure and document both animation tables and all eight mapping frames with named freed/hologram scripts, pose roles, empty pulse frames, and the five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered family, and reduces active address-derived labels by twelve, from 16,455 to 16,443. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M68 | Complete | Recover the shared Collision Chaos robot-generator family used by all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Replace its address-derived dispatcher, lifecycle joins, inactive/solid exits, explosion completion and allocation exits, float helper, explosion-schedule label, and base-tile table label with functional names; document its Past-only activation, reused object fields, one-pixel float cadence, `[act * 4 + time_zone]` art selection, and the twenty-one three-byte explosion records plus `$FF` terminator. Structure the shared animation and mapping assets with a named active-Past script, eight functional state/frame labels, and an explicit five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered family, and reduces active address-derived labels by fifteen, from 16,443 to 16,428. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M69 | Complete | Recover the shared Collision Chaos Metal Sonic projector and linked hologram-child family used by all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Replace its address-derived dispatcher, parent/hologram lifecycle states, delete/solid/explosion joins, child-size/animation setup, and explosion-schedule label with functional names; document the projector's reused state fields, four spawned child contracts, 16-bit parent-slot links, parent invalidation byte, sixty-frame destruction delay, and twelve three-byte explosion records followed by a `$FF` terminator and explicit alignment byte. Structure both hologram animation scripts and all six projector/beam/Metal Sonic mapping frames with functional labels and the five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered family, and reduces active address-derived labels by eleven, from 16,428 to 16,417. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M70 | Complete | Converge the shared object time-zone checks across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route every R3 graph through `Level/Object Time Check.asm`, expose the recovered `DestroyInGoodFuture` entry as a byte-neutral colocated label, select the recovered `CheckObjectDespawn` provider behind an explicit R3 switch, and invert the earlier Ga compatibility bridge only when a graph still uses the legacy provider. Preserve `common/time_check.asm` because Tidal Tempest, Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness continue to consume it. | Audit/check reports zero missing or case-mismatched includes; the repository-wide address-label total is unchanged because both semantic and legacy modules remain active in distinct graphs. The first Japan build bounded asm68k's unresolved forward-`EQU` behavior and the recovered despawn-provider name; after moving both compatibility decisions to the semantic module, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M71 | Complete | Converge the shared Game Over/Time Over object and mapping graph across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route every R3 graph through `Level/_Objects/Game Over/Main.asm`, bind the recovered object-index and allocator/helper ABI behind an explicit R3 switch, and replace the canonical mapping file's four address-derived frame labels with functional Game, Time, and Over text roles plus its five-byte sprite-piece schema. Preserve `objects/game_over.asm` because Tidal Tempest, Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness continue to consume it. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the canonical Game Over family, and reduces active address-derived labels by four, from 16,417 to 16,413. The first Japan build bounded the recovered `SpawnObject` allocator name; after selecting it behind the R3 switch, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M72 | Complete | Converge the shared Title Card behavior across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route every R3 graph through `Level/_Objects/Title Card/Main.asm`; bind the recovered object-index, allocator, draw/delete services, and R3-specific placement/mapping providers behind an explicit switch; and document the eight-byte placement-record and five-byte sprite-piece schemas. Preserve `objects/title_card.asm` because Tidal Tempest, Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness continue to consume it, and retain the distinct `r3/title_card_data.asm` and `sprites/r3/title_card.asm` visuals rather than substituting another zone's title-card data. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical behavior or retained R3 data providers. The repository-wide address-label total remains 16,413 because the later-zone legacy provider is still active. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M73 | Complete | Converge the complete shared Results family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route every R3 graph through `Level/_Objects/Results/Main.asm`, bind the recovered object-index plus allocator, draw, sound, Z80, score, and fade-service ABI behind an explicit switch, and preserve the region/demo-specific timers, tally paths, special-stage warp sound, time-stone logic, and Stardust Speedway Act 3 art selection. Replace the canonical mapping file's ten address-derived frames and fragmented byte dump with functional bad/good header, score, act-number, and SSZ3 score records using documented five-byte sprite pieces and explicit alignment. Preserve `objects/results.asm` because Tidal Tempest, Quartz Quadrant, Wacky Workbench, Stardust Speedway, and Metallic Madness continue to consume it. | Audit/check reports zero missing or case-mismatched includes and no address-derived labels in the canonical Results family. The structured mapping rewrite reduces active address-derived labels by ten, from 16,413 to 16,403, and condenses the mapping source from 629 to 190 lines without unexplained data emission. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M74 | Complete | Recover the paired Collision Chaos Act 3 boss-entry barriers used by `R33C__.MMD` and `R33D__.MMD`. Name the four-state initialization, collision, retraction, and boss-active dispatcher; document and apply the retract timer, shared flag byte, raised-Y target, 16.16 vertical speed, and linked-object slot fields; and recover the arena-threshold boss trigger plus linked-barrier deletion flow. Preserve the following boss-machine palette-flash helper as the explicit boundary for the next milestone. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by eight, from 16,403 to 16,395. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M75 | Complete | Recover the Collision Chaos Act 3 boss-machine core used by `R33C__.MMD` and `R33D__.MMD`: the damage-flash palette helper, camera-focus lock, six-state parent dispatcher, linked Eggman/gear/bomb-launcher allocation, clamped player-Y tracking, and combat-entry setup. Name the shared flash timer, control flags, target Y, 16.16 vertical speed, child links, attack count, and encounter phase fields; give all six dispatcher destinations stable functional entry names while leaving their later combat bodies for the next milestones. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by fourteen, from 16,395 to 16,381. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M76 | Complete | Recover the Collision Chaos Act 3 boss-machine active combat unit used by `R33C__.MMD` and `R33D__.MMD`: player-position eligibility for exit barriers, the four player-Y thresholds and four corresponding barrier spawn heights, active collision handling, three hit phases, linked upper-child transitions, defeat score award, and future-dependent sub-CPU command. Name the combat timer and apply the existing attack-count, encounter-phase, upper-child-link, flash-timer, and target-Y fields while preserving the following defeat-rise helper as the explicit boundary for the next milestone. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by thirteen, from 16,381 to 16,368. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M77 | Complete | Recover the complete Collision Chaos Act 3 boss hit-reaction and defeat sequence used by `R33C__.MMD` and `R33D__.MMD`: final-barrier eligibility, three phase-specific hit-explosion patterns, target-Y recovery, the defeated rise and exit wait, linked gear cleanup, and the timed arena explosion scheduler shared with both entry barriers. Name the shared arena actor timer/flags/link contract and the boss rise counter; classify every explosion pattern as an interval/count header followed by signed X/Y offsets; and preserve the following Eggman pilot object as the explicit boundary for the next milestone. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by twenty-two, from 16,368 to 16,346. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M78 | Complete | Recover the complete Collision Chaos Act 3 Eggman pilot child family used by `R33C__.MMD` and `R33D__.MMD`. Name its five-state initialization, boss-follow, vertical rebound, upward escape, and rightward escape dispatcher; document and apply its parent link, 16.16 vertical speed, reused initial-Y/target-X field, boss-phase dependency, and stack-discarding deletion contract. Correct the boss-parent ABI by identifying `$34` as the linked gear and `$36` as Eggman, then structure the five animation scripts and ten mapping frames with functional roles plus explicit animation and five-byte sprite-piece schemas. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered Eggman behavior, and reduces active address-derived labels by six, from 16,346 to 16,340. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M79 | Complete | Recover the complete Collision Chaos Act 3 linked gear child family used by `R33C__.MMD` and `R33D__.MMD`. Name its initialization, boss-follow, draw, and encounter-finished exits; document and apply its parent link and shared arena flag; and expose the parent boss movement flag that gates its animation. Structure the two primary/alternate spin scripts and three rotation mapping frames with explicit animation-record and one-piece mapping schemas while avoiding an unsupported physical rotation-direction claim. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered gear behavior, and reduces active address-derived labels by three, from 16,340 to 16,337. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M80 | Complete | Recover the complete Collision Chaos Act 3 bomb-launcher and bomb projectile family used by `R33C__.MMD` and `R33D__.MMD`. Name the launcher's parent-gated three-state dispatcher, 120-frame fire timer, alternating-side random launch setup, eight-step firing sequence, and deletion path. Name the bomb's six-state lifecycle, 240-frame lifetime, accelerated/capped 8.8 motion, four-way terrain bounce, player knockback, sounded/silent detonation entries, explosion conversion, and deletion; classify its unreachable duplicate-acceleration instructions as executable leftover rather than data. Structure the five launcher and two bomb mapping frames with explicit five-byte sprite-piece schemas. | Audit/check reports zero missing or case-mismatched includes and reduces active address-derived labels by twenty-three, from 16,337 to 16,314. The first Japan build exposed a local-label scope split at the classified executable leftover; after promoting the position join to a functional global label, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M81 | Complete | Recover the complete Collision Chaos Act 3 exit-barrier family used by `R33C__.MMD` and `R33D__.MMD`. Name its full-width and partial-width initialization entries, shared setup, manual extension stop, and persistent top-solid state; document and apply the target-frame field; and preserve the direct solid-state fast path. Structure the animation as an explicitly manual-ended extension and classify all eight mapping frames as one through eight identical 16-pixel segments using the documented five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, no address-derived labels in the recovered exit-barrier behavior, and reduces active address-derived labels by four, from 16,314 to 16,310. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M82 | Complete | Complete the Collision Chaos Act 3 boss visual family for `R33C__.MMD` and `R33D__.MMD`. Replace the entry barrier's generic animation and mapping labels with conservative stationary/transition and normal/alternate roles, documenting both record schemas. Structure the boss machine's seven frames as three observable normal/flash damage pairs plus the bare hull; document the five-byte piece format and the damage helper's frame-bit pairing contract without inventing unsupported gameplay semantics. | Audit/check reports zero missing or case-mismatched includes and no generic numbered labels in the three recovered asset sources. The active address-label total remains 16,310 because these were local numbered data labels rather than address-derived labels. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M83 | Complete | Recover the complete Collision Chaos Act 3 Drain object family used by `R33C__.MMD` and `R33D__.MMD`. Name the subtype-selected switch, indicator-light, and moving-block dispatchers and lifecycle states; document and apply the switch's 128-frame cooldown, player overlap latches, even stage progression, linked-child slots, three indicator Y positions, block raise/lower commands, and child-to-parent validity contract. Structure the two switch animations, two block animations, and all switch/light/block mappings with functional roles, control-byte behavior, and explicit five-byte sprite-piece schemas. | Audit/check reports zero missing or case-mismatched includes, no address-derived or generic numbered labels in the recovered Drain family, and reduces active address-derived labels by sixteen, from 16,310 to 16,294. The first Japan build exposed a local `.Reset` label whose scope was split by the newly named position table; after promoting it to `DrainSwitch_Reset`, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including the two affected Act 3 Collision Chaos outputs. |
| M84 | Complete | Converge the inactive Collision Chaos teleporter object across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine graphs through the canonical semantic module, retain its compatibility aliases behind the existing explicit switch, classify its sole mapping record as an empty frame, and remove the redundant legacy source. Document the observed retail behavior precisely: the object immediately tail-jumps to deletion despite retaining an object-index slot, debug entry, mapping symbol, and loaded art. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, and one canonical teleporter provider across all Collision Chaos graphs. The active address-label total remains 16,294 because neither duplicate stub used address-derived labels. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M85 | Complete | Converge and semantically complete the Collision Chaos Pocket family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine graphs through the canonical provider's explicit recovered ABI path and remove the duplicate source. Name the negative-subtype parent and linked child dispatchers, player capture, effect spawn, opening, two award states, closing, release, cooldown, and parent-follow lifecycle; document the successful-Good-Future suppression, player-control flags, timers, seven-award counter, linked-slot contract, and parent-ID validation. Structure the pulsing child animation and all six parent/child mapping frames with functional roles and the five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no numbered routine or asset labels in the canonical Pocket family, and reduces active address-derived labels by twenty, from 16,294 to 16,274. The first Japan build bounded two helper-name dependencies (`FindObjSlot` and `CheckObjDespawn`) that R31A had supplied through an unrelated checkpoint switch; after making the recovered Pocket ABI call its native `SpawnObject` and `CheckObjectDespawn` providers directly, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M86 | Complete | Converge and semantically complete the Collision Chaos Tentou and bomb family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine graphs through the canonical provider's explicit recovered ABI path and remove the duplicate source. Name Tentou's floor acquisition, player trigger, movement, wall-rise, gap-crossing, bobbing, optional bomb-drop, and retained unused range-check roles; name the bomb's fall, 120-frame arm delay, 120-frame animated fuse, collision detonation, and explosion conversion states. Document the reused 16.16 velocity/timer field, base-Y and 16-tick phase fields, subtype-selected art/drop behavior, floor/wall service contract, and successful-Good-Future removal. Structure both three-frame flight variants and the two-frame bomb fuse with functional labels and five-byte sprite-piece schemas. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no numbered routine or asset labels in the canonical Tentou family, and reduces active address-derived labels by twenty, from 16,274 to 16,254. The first Japan build exposed five floor/wall helper names that R31A had inherited from unrelated semantic switches; after binding private Tentou service names to the recovered `CheckBlock*` providers in the legacy ABI, fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M87 | Complete | Converge and semantically complete the Collision Chaos Spike Chain family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Extend the canonical recovered ABI from its original Present-only three-child form to the exact `STAGE_TIME + 2` layout: two Past, three Present, and four Future children. Route the remaining nine graphs through that provider, remove the duplicate source, name the parent/child dispatchers, rotation state, copied-position state, angle, signed angular velocity, four possible child links, and parent link, and bind native recovered allocation, sine/cosine, and despawn services. Classify the parent hub and hazardous child as one-piece mappings with the five-byte sprite-piece schema. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no numbered routine or asset labels in the canonical Spike Chain family, and reduces active address-derived labels by seven, from 16,254 to 16,247. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, proving all three time-period layouts across all ten Collision Chaos outputs. |
| M88 | Complete | Converge and complete the Collision Chaos Spikes family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine graphs through the canonical provider and remove the duplicate source; replace its generic dispatcher/main labels with routine-table and update roles. Preserve and document the subtype low-bit orientation/property selection, movement-enable bit, attached-parent slot plus signed byte offsets, saved base coordinates, custom R3 despawn anchor, top-spike hurt path, 8-pixel movement steps, 32-pixel travel, and 60-frame endpoint delays. Structure the Up, Down, Left, and Right mappings as one-piece five-byte records. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no numbered routine or asset labels in the canonical Spikes family, and reduces active address-derived labels by fifteen, from 16,247 to 16,232. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M89 | Complete | Converge and complete the combined Collision Chaos Block and Hidden Block family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine graphs through the canonical combined provider and remove both duplicate sources. Name each routine table and update state; document the visible block's saved coordinates, 8.8 position/velocity, timer/direction fields, subtype-selected child contract, four duration/acceleration/initial-velocity motion records, time-period movement gate, and custom R3 despawn anchor. Document the hidden block's subtype-selected mapping, child type and signed vertical direction, plus the proximity helper's 8-pixel steps and 0-to-32-pixel clamp. Structure the visible and hidden mappings as named one-piece five-byte records. | Audit/check reports zero missing or case-mismatched includes, two fewer tracked duplicate sources, no generic dispatcher/main or numbered mapping labels in the canonical family, and reduces active address-derived labels by eighteen, from 16,232 to 16,214. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M90 | Complete | Converge and semantically complete the Collision Chaos Metal Platform family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live graphs through the canonical provider, retain the two unused `Act 1/2 Shared.asm` graphs unchanged, and remove the duplicate source. Name the object and subtype dispatchers, stationary, horizontal, vertical, delayed-drop, player-weight bobbing, and top-solid roles; document and apply the saved coordinates, signed 8.8 velocity/acceleration, motion timer/step, drop routine, bob angle, and bob-enable fields. Name the horizontal and vertical tables by starting direction and the three drop tables by subtype, preserving their documented four-byte record schema; classify the mapping as one frame containing two five-byte pieces. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no recovered routine-number, raw private-field, or numbered mapping labels in the canonical family, and reduces active address-derived labels by twenty-seven, from 16,214 to 16,187. The first Japan assembly exposed that R31A's semantic Drum Platform and the other nine graphs publish the same later player-slot helper under different names; a private ABI binding preserves both targets. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M91 | Complete | Converge and semantically complete the Collision Chaos Door family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live graphs through the canonical provider, remove the duplicate recovered source and its now-unreachable byte-identical external mapping, and preserve the two unused semantic shared graphs. Name the routine table, update and opening-offset roles; document and apply the base coordinates, stored delay, opening offset, and one-shot timer fields. Preserve the low-subtype delay lookup, sign-bit-selected positive/negative-X opening, player-Y-controlled 16-pixel steps, 64-pixel clamp, subtype-zero closed-state delay/timer latch, top-solid behavior, custom R3 despawn, and private ABI binding to the later Drum player-slot helper. Classify the canonical mapping as one frame containing two five-byte pieces. | Audit/check reports zero missing or case-mismatched includes, two fewer tracked duplicate files, no recovered routine-number or numbered mapping labels in the canonical family, and reduces active address-derived labels by eight, from 16,187 to 16,179. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M92 | Complete | Converge and semantically complete the Collision Chaos Fire Shooter family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live graphs through the canonical provider and remove the duplicate source while preserving the two unused semantic shared graphs. Name the emitter, flame, and projectile dispatchers and lifecycle states; document and apply their shared base coordinates plus the flame activation timer/height and projectile direction/acceleration/burnout fields. Preserve the 120-pixel trigger, time-period and Good-Future projectile gates, 8-pixel raising, 4-pixel lowering, 32-pixel height, player launch, paired projectile spawn, 64-pixel flight, acceleration, collision disable, 30-frame burnout, and custom R3 despawn contracts. Correctly classify and name the five flame/left/right flight/burnout animation scripts and seven mapping entries, including the shared empty frame and its ignored five-byte zero tail padding. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no recovered routine-number, raw private-field, or numbered animation/mapping labels in the canonical family, and reduces active address-derived labels by thirty-four, from 16,179 to 16,145. The first Japan assembly exposed allocation/despawn names inherited by R31A from an unrelated checkpoint switch in addition to the known later Drum helper split; private ABI bindings now call the native recovered providers in the other nine graphs. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M93 | Complete | Converge and semantically complete the Collision Chaos Drum Platform family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live graphs through the canonical provider and remove the duplicate source while preserving the two unused semantic shared graphs. Correct the subtype contract from a mapping-frame claim to the actual starting motion-step index; name the early non-solid, solid, and late non-solid phases, top-solid helper, motion-step loader, and adjacent step timer/motion-step fields. Classify all eighteen three-byte motion records as signed whole-pixel velocity written into the high byte of 8.8 velocity, collision radius, and mapping frame held for four eligible ticks; preserve the Present-only radius adjustments, animated-art timer gate, and word clear that resets both adjacent state bytes. Name all twelve mapping frames by bounce-cycle role, document their two-piece schema, and classify the ignored five-byte piece-shaped tail behind the zero-count hidden frame. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no recovered routine-number, incorrect frame-state, or numbered mapping labels in the canonical family, and reduces active address-derived labels by twenty-two, from 16,145 to 16,123. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs and the three preceding object families' PC-relative calls to the historically placed player-slot helper. |
| M94 | Complete | Converge the Collision Chaos saved-object despawn helper across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live graphs through the canonical provider and remove the address-labelled duplicate while preserving the two unused semantic shared graphs. Retain the documented three-byte saved-object record, time-period byte selection, loaded-bit clear, zero-ID deletion path, and exact post-Drum placement required by eight canonical obstacle branches. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, one canonical `DespawnObjectR3` provider across all Collision Chaos graphs, and reduces active address-derived labels by one, from 16,123 to 16,122. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs and the eight incoming obstacle branches. |
| M95 | Complete | Converge and semantically complete the Collision Chaos KamaKama and sickle family across all ten `R31A-D`, `R32A-D`, and `R33C-D` outputs. Route the remaining nine live outputs through the canonical recovered-ABI provider and remove the duplicate source while preserving the two unused semantic shared graphs. Name the parent's floor acquisition, player watch, attack wait, recovery wait, walking, and turn-wait states plus the sickle's launch wait and flight states. Document and apply the parent timer/stored-subtype fields and the sickle parent link, launch delay, copied flags, despawn anchor, gravity, and damage timer. Preserve the non-negative subtype's paired sickle attack, the negative subtype's no-projectile alternate presentation and half-speed walk, parent-slot validation, collision/deflection behavior, and custom anchored despawn. Name both eight-frame parent mapping sets by animation role and the six sickle rotation frames by observed tile-flip role, retaining the documented count-prefixed five-byte piece schema. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no recovered routine-number or numbered mapping labels in the canonical family, and reduces active address-derived labels by thirty-two, from 16,122 to 16,090. The first Japan build exposed allocation, floor-distance, normal-despawn, and anchored-despawn helper names inherited by R31A from unrelated semantic switches; private ABI bindings now select the native recovered providers in the other nine outputs. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten affected Collision Chaos outputs. |
| M96 | Complete | Converge the Level End code graph across all ten Collision Chaos outputs by routing the remaining nine outputs through the canonical flower-capsule, big-ring/flash, goal-post, signpost, and results implementation. Bind allocation, gravity, floor-distance, and despawn services explicitly for the recovered ABI. Retain `objects/stage_end.asm` because Wacky Workbench and other legacy graphs still consume it, and leave the canonical Level End animation and mapping records explicitly unresolved for the next milestone. | Audit/check reports zero missing or case-mismatched includes and no change to the 16,090 active address-derived-label total because the legacy provider remains active elsewhere. The first Japan build exposed the out-of-scope Wacky Workbench consumer, after which the source was retained. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region, including all ten Collision Chaos outputs. |
| M97 | Complete | Structure the canonical Level End data shared by Collision Chaos and the other semantic level graphs. Replace the four residual address-derived animation labels with named capsule idle/seed, big-ring rotation, flash expansion/deletion, and signpost spin scripts; document delay and `$FF` restart or `$FC` deletion commands. Replace nineteen address-derived mapping frames with conservative capsule state, seed phase, big-ring view, signpost face/turn, and goal-post roles; retain the eight already named flash expansion phases. Consolidate all mappings into count-prefixed five-byte sprite pieces and express each genuine zero alignment byte with `even`. | Audit/check reports zero missing or case-mismatched includes and no residual address-derived or generic numbered labels in the eight canonical data sources. The active address-label total remains 16,090 because the replaced names were local data labels outside the counted families. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region, proving the shared records across every live consumer. |
| M98 | Complete | Converge and semantically complete the Collision Chaos freed-animal and projector-hologram family across all ten outputs. Route all nine entry graphs through the canonical module, remove the R3-only duplicate, and expose its recovered object, animation, mapping, and base-tile symbols for later object-index/debug consumers. Name the flying routine table and orbit state plus the ground routine table, bounce, and turn states; document and apply the saved orbit centre, phase/signed step, 16.16 ground velocities, projector parent link, and parent-private validity flag. Preserve subtype family selection, negative-subtype holograms, orbit scales, bounce gravity/landing state progression, act/time-zone art selection, and the already structured hologram animation/mapping records. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, and no generic dispatcher/main labels in the canonical family. The active address-label total remains 16,090 because the removed legacy provider already used functional labels. The first Japan comparison localized two bytes in every Collision Chaos output to the dormant canonical module's incorrect `$29` `oSubtype2` name; tracing the recovered projector contract proved both checks target the parent-private `$3F` flag, now named `oCCAnimalParentFlag`. Fresh displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M99 | Complete | Converge and semantically complete the Collision Chaos Robot Generator across all ten outputs. Route all nine entry graphs through the canonical shared implementation followed by the zone-specific tile table, remove the R3-only duplicate, gate the recovered compatibility alias, and expose the recovered object/animation/mapping/schedule symbols for later index and debug consumers. Name the routine table, active-Past, exploding, rebuild-delay, and floating roles; document and apply the reused float/explosion timer, schedule cursor, saved base Y, and signed float-step fields. Preserve the Past/bad-future activation gate, one-pixel/eight-frame float with 32-frame reversals, collision destruction, Good-Future transition, score/solid-object contracts, eight-tick rebuild, act/time-zone tiles, structured animation/mappings, and classify the explosion schedule as trigger-tick plus signed X/Y records terminated by `$FF`. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no raw private-field or generic dispatcher/main labels in the canonical family, and no change to the 16,090 active address-label total because the removed provider already used functional labels. Fresh displayless Steam Proton Experimental builds completed for Japan, USA, and Europe on the first attempt, and all 133 comparisons matched in each region. |
| M100 | Complete | Converge and semantically complete the Collision Chaos Projector and Metal Sonic hologram family across all ten outputs. Route all nine entry graphs through the canonical shared provider, remove the R3-only duplicate, gate the recovered compatibility alias, and expose the recovered object, animation, mapping, and explosion-schedule symbols. Name the projector routine table, solid, explosion, final-delay, and hologram roles; document and apply the explosion timer/schedule, parent link, child-invalid flag, and explosion-object flag fields. Preserve the Collision Chaos allocation, despawn, graphics-queue, object-ID, tile, child-position, animation, mapping, and three-byte explosion-record contracts behind an explicit legacy ABI. | Audit/check reports zero missing or case-mismatched includes, one fewer tracked duplicate source, no raw private-field or generic dispatcher/main labels in the canonical family, and no change to the 16,090 active address-label total because the removed provider already used functional labels. The first Japan assembly exposed the explosion object's private flag name; the first comparison then localized a four-byte growth to two tile writes, proving that Collision Chaos uses direct immediate-to-object instructions where the Palmtree implementation stages through `d0`. The explicit ABI path preserves both layouts. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M101 | Complete | Converge and semantically complete Collision Chaos animated-tile updates across all ten outputs. Route Acts 1 and 3 through the canonical shield-art tail call and consolidate Act 2's Past no-op plus Present/Good/Bad Future two-frame updater behind `CC_VARIANT`; remove all ten recovered duplicate modules and gate the inverse compatibility alias. Name the frame-selection table, copy loop, work-RAM timer/frame fields, variant-specific source windows, DMA lengths, and VRAM destinations. Classify all twelve active source labels as raw 4bpp primary/secondary tile windows and document the retained 768-byte over-copy versus the 320-byte and 192/256-byte DMA prefixes. | Audit/check reports zero missing or case-mismatched includes, ten fewer tracked duplicate sources, two fewer unreachable sources, no address-derived code/data labels in the canonical updater family, and reduces active address-derived labels by twenty-seven, from 16,090 to 16,063. The first Japan assembly exposed the recovered `LoadPowerupArt` versus semantic `LoadShieldArt` provider split; a private binding preserves both. A second assembly exposed local-label scope after the copy helper was named, so the shared early-return label was promoted without changing layout. Fresh isolated displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M102 | Complete | Converge and structure Collision Chaos stage descriptors and graphics-loading lists across all ten outputs. Route Acts 1 and 2 through their canonical variant providers, add the missing 16-byte stage descriptors, create a canonical Act 3 Good/Bad Future provider with named boss and escape lists, gate the inverse list alias, and remove all ten recovered duplicate modules. Document each PLC as a count-minus-one word followed by six-byte art-pointer/VRAM-destination records; preserve all 16/17/19-slot dispatch tables, time-period asset selections, section initialization/update lists, intro/animal/projector/results/signpost lists, and Act 3 boss/escape loads. | Audit/check reports zero missing or case-mismatched includes, ten fewer tracked duplicate sources, three fewer unreachable sources, and reduces active address-derived labels by three, from 16,063 to 16,060, removing the final address-named lists in this family. The first Japan assembly bounded the dormant R3 common-art namespace, which is now privately bound only for the legacy PLC ABI. The first comparison then proved that the dormant canonical Act 1 Future table had only nine fallback slots rather than the required twelve; correcting that defect restored the 19-slot contract. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M103 | Complete | Structure the active Collision Chaos Act 2 layout-pointer data shared by `R32A__.MMD` through `R32D__.MMD`. Replace four repeated 36-word lists with one documented provider expressing the loader's twelve-slot foreground/background/null schema repeated for three section indices. Name the Green Hill fallback layouts and the three separately emitted four-byte null records by their observed table roles, while retaining each record at its historical address. Leave the adjacent large `Padding 1` and `Padding 2` streams explicitly unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because this milestone replaces generic `Unk` names rather than address-derived labels. The first Japan comparison showed that a historical `StageMapFg-*` expression used the table base only in the first row; changing the shared source to the explicit `StageMaps` base preserved all three repetitions. Fresh isolated displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M104 | Complete | Begin exact-range recovery of the Japan/Europe R32A pre-chunk block used by `R32A__.MMD`. Prove that the 3,356-byte `$20F2E4-$20FFFF` range fills the boundary before `StageChunks` and is retained program/data rather than padding; rename its provider accordingly. Convert `$20F334-$20F42D` from raw bytes into an orphaned title-card initialization tail, four slide states, and PLC-wait state with named object fields and control flow. Document that the copied `dbf` target lands in preceding retained data, that no live code points to these entries, and that its historical absolute draw/delete operands no longer name routine entries. Keep the surrounding data and following Results fragment explicitly unresolved by exact range. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because all new names are functional. Binary-reference analysis found no live pointer into the recovered executable range and distinguished later live mapping/table references from executable entries. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M105 | Complete | Recover the complete orphaned Results unit at `$20F42E-$20F6ED` in the Japan/Europe R32A retained pre-chunk block. Express its five-entry dispatcher, delayed PLC load, player/camera-gated three-object spawn loop, Good/Bad and Stardust Speedway Act 3 mapping selection, movement, bonus tally and sounds, score addition, next-act/zone transition, and Good Future bookkeeping as real 68000 source. Name the reused destination-X/timer fields and all internal states while preserving historical absolute service operands that no longer match today's semantic entry addresses. Structure `$20F6EE-$20F705` as three eight-byte initialization records and `$20F706-$20F72D` as four five-offset mapping tables; leave the frame bodies from `$20F72E` onward unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the recovered unit uses functional names. Binary-reference analysis found no live pointer to the retained dispatcher, while its internal branches and table references remain coherent. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M106 | Complete | Complete the orphaned Results data unit at `$20F706-$20F973` in the Japan/Europe R32A retained pre-chunk block. Replace the four unexplained five-word offset lists with label-relative Bad, Bad Stardust Speedway Act 3, Good, and Good Stardust Speedway Act 3 mapping tables. Structure and name all ten referenced frames: the Bad/Good headers, three act-number variants for each future state, the standard score panel, and its Stardust Speedway Act 3 variant. Document every frame as a count followed by five-byte Y/size/tile/X sprite pieces and express each alignment byte with `even`; leave the following retained title-card records explicitly unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because all mapping names are functional. The fresh Europe listing confirms exact starts at `$20F706`, `$20F72E`, `$20F758`, `$20F78C`, `$20F7C0`, `$20F7F4`, `$20F83C`, `$20F874`, `$20F8AC`, `$20F8E4`, `$20F92C`, and the next unresolved range at `$20F974`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M107 | Complete | Complete the retained title-card data unit at `$20F974-$20FACD` in the Japan/Europe R32A pre-chunk block. Structure its eight placement records as initial Y/X, destination X, and packed mapping-frame/slide-delay words; document the three placements that reuse the zone-name frame and the act-indexed selection of frames 4 through 6. Replace the nine unexplained mapping offsets with label-relative backdrop, headline, zone-name, subtitle, three act-number, zone-label, and zone-number frames; express every frame as a count followed by five-byte sprite pieces and explicit alignment. Classify the final six bytes as an orphaned absolute-jump trampoline, while leaving the retained animation and PLC-like records from `$20FACE` onward unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because all new names are functional. The fresh Europe listing confirms the placement table at `$20F974`, mapping table at `$20F9B4`, frames at `$20F9C6`, `$20F9E6`, `$20F9F2`, `$20F9FE`, `$20FA18`, `$20FA3C`, `$20FA60`, `$20FA84`, and `$20FAA8`, the trampoline at `$20FAC8`, and the next unresolved range at `$20FACE`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M108 | Complete | Recover the complete orphaned Collision Chaos Act 1 Present stage descriptor and graphics-loading graph retained at `$20FACE-$20FCF3` in the Japan/Europe R32A pre-chunk block. Express the three historical stage-resource pointers, layout flags, and palette IDs by their real widths. Replace the nineteen raw list offsets with label-relative Stage, Standard, four section, four update, intro, Tentou, Results, Signpost, and repeated fallback entries; structure all fourteen unique bodies as count-minus-one records followed by six-byte art-address/VRAM-destination pairs. Document that this is an unreachable historical snapshot whose embedded absolute art addresses cannot safely be rebound to the live graph, and leave the later retained units from `$20FCF4` onward explicitly unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. The fresh Europe listing confirms the descriptor at `$20FACE`, list index at `$20FADE`, bodies at `$20FB04`, `$20FB12`, `$20FB6E`, `$20FB82`, `$20FBCC`, `$20FC10`, `$20FC5A`, `$20FC62`, `$20FCAC`, `$20FCB4`, `$20FCC2`, `$20FCD0`, `$20FCD8`, and `$20FCE0`, with the next unresolved byte at `$20FCF4`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M109 | Complete | Recover the complete orphaned Palmtree Panic Act 3 Bad Future stage descriptor and graphics-loading graph retained inside Japan/Europe R32A. Structure the historical stage-art, block, chunk, palette, and layout fields at `$20FDF0`; replace its nineteen raw offsets with label-relative Stage, Main, Section, Boss, Results, Capsule, and repeated Section entries; and express all six unique PLC bodies as count-minus-one records followed by six-byte art-address/VRAM-destination pairs. Identify the snapshot by its byte-identical placement in the retained `R13D` pre-chunk source and comparison with the semantic Palmtree Act 3 graph, while preserving historical absolute pointers that no longer map safely onto the live layout. Recover the jump trampolines at `$20FDEA` and `$20FECC`, and keep the intervening `$20FEAA-$20FECB` records plus following Good Future graph separately unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. The fresh Europe listing confirms the Bad Future entry at `$20FDEA`, descriptor at `$20FDF0`, index at `$20FE00`, bodies at `$20FE26`, `$20FE34`, `$20FE72`, `$20FE8C`, `$20FE9A`, and `$20FEA2`, the next unresolved byte at `$20FEAA`, and the Good Future entry at `$20FECC`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M110 | Complete | Recover the complete orphaned Palmtree Panic Act 3 Good Future stage descriptor and graphics-loading graph retained at `$20FED2-$20FF8B` inside Japan/Europe R32A. Structure its historical stage-art, block, chunk, palette, and layout fields; replace nineteen raw offsets with label-relative Stage, Main, Section, Boss, Results, Capsule, and repeated Section entries; and express all six unique bodies as count-minus-one records followed by six-byte art-address/VRAM-destination pairs. Relate the snapshot to the preceding Bad Future unit while preserving its distinct stage and section-art pointers, and prove that the graph ends cleanly before a separate truncated PLC fragment at `$20FF8C`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. The fresh Europe listing confirms the descriptor at `$20FED2`, index at `$20FEE2`, bodies at `$20FF08`, `$20FF16`, `$20FF54`, `$20FF6E`, `$20FF7C`, and `$20FF84`, and the next unresolved byte at `$20FF8C`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M111 | Complete | Classify the final 116 bytes before the `$210000` chunk boundary as a retained Japan/Europe `DEMO11A` graphics-list slice, using its complete copies in the Palmtree Panic retained-data sources. Express `$20FF8C-$20FFA1` as a main-list suffix that begins halfway through the historical explosion-art record and then contains three complete art/VRAM pairs. Structure `$20FFA2-$20FFEB` as a complete twelve-record section PLC and `$20FFEC-$20FFF3` as a complete Results PLC. Express `$20FFF4-$20FFFF` as the prefix of a three-record Signpost PLC that retains one complete signpost-art record and the following big-ring pointer, but is cut off before that record's VRAM destination by `StageChunks`; do not invent the missing bytes. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The fresh Europe listing confirms starts at `$20FF8C`, `$20FFA2`, `$20FFEC`, and `$20FFF4`, followed immediately by `StageChunks` at `$210000`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M112 | Complete | Identify `$20FEAA-$20FECB` inside Japan/Europe R32A as the retained tail of Quartz Quadrant's three-fragment break-wall tables by matching the exact values and consumer logic in `objects/r5/break_wall.asm`. Express the slice as the remaining centre/lower signed Y offsets after the omitted `-16` prefix, two three-byte frame sets selected by subtype, and three signed 16.16 X/Y velocity pairs used for the spawned fragments. Document both the missing prefix and the selection/record contracts rather than treating the bytes as generic tables. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The fresh Europe listing confirms the Y-offset tail at `$20FEAA`, frames at `$20FEAE`, velocity pairs at `$20FEB4`, and the following retained trampoline at `$20FECC`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M113 | Complete | Classify `$20FCF4-$20FDE9` inside Japan/Europe R32A as a retained `DEMO11A` Palmtree Panic Act 1 Present graphics-list slice. Express the truncated Cam 4 Full tail as the surviving Animals VRAM destination after its omitted art pointer, followed by five complete art/VRAM records. Structure the following Cam 5 Full, Cam 1-5 Incremental, Results, and Signpost bodies as eight complete count-minus-one PLCs, retaining their historical absolute art pointers and the graph's distinct `$0020DB4A` big-ring-flash address. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The recovered boundaries are `$20FCF4`, `$20FD14`, `$20FD34`, `$20FD5A`, `$20FD6E`, `$20FD9A`, `$20FDC0`, `$20FDCE`, and `$20FDD6`, ending immediately before the retained trampoline at `$20FDEA`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. The sole remaining unresolved Japan/Europe R32A retained range is `$20F2E4-$20F333`. |
| M114 | Complete | Finish the Japan/Europe R32A retained pre-chunk block by classifying `$20F2E4-$20F317` as the tail of a historical Collision Chaos Act 1 Present section PLC. Document its match to the live Section 1 destination/order contract, its historical two-record Pocket-art split, and its truncation halfway through the first one-way-barrier art pointer. Structure `$20F318-$20F31F` as a complete Results PLC and `$20F320-$20F333` as a complete Signpost PLC, retaining all historical absolute pointers. Preserve `RetainedTitleCardInitLoopTarget` at `$20F304` on the spike-chain record's `$77A0` destination because the following orphaned title-card initializer still branches into that retained data. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The first Japan comparison localized one differing branch-displacement byte to the loop label having moved two bytes forward; restoring the historical target produced fresh isolated displayless Steam Proton Experimental builds for Japan, USA, and Europe with all 133 comparisons matching in each region. The fresh listing confirms the section tail at `$20F2E4`, loop target at `$20F304`, Results at `$20F318`, Signpost at `$20F320`, and following executable fragment at `$20F334`. The entire `$20F2E4-$20FFFF` Japan/Europe R32A retained pre-chunk block is now classified by exact range. |
| M115 | Complete | Begin exact-range recovery of the separate USA R32A pre-chunk block by structuring `$20F2D6-$20F309` as the same historical Collision Chaos Act 1 Present section-PLC tail found in the Japan/Europe snapshot. Document its truncated one-way-barrier pointer, two-record Pocket-art split, and Section 1 destination/order contract. Structure the complete Results PLC at `$20F30A-$20F311` and Signpost PLC at `$20F312-$20F325`, preserving the USA snapshot's distinct `$0020DC60` big-ring-flash pointer and the `$20F2F6` target embedded for the following title-card copy. Leave `$20F326-$20FFFF` explicitly unresolved by exact range. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. Inclusion tracing confirms that only USA `R32A__.MMD` selects this provider through `r3/data_2a.asm`, while Japan/Europe select their independently structured provider. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. The subsequent M116 listing corrected these addresses from the initially documented JE-relative values without changing M115's already byte-matched emission. |
| M116 | Complete | Recover the orphaned USA title-card executable copy at `$20F326-$20F41F`. Replace raw bytes with the initialization tail, vertical/horizontal slide-in states, delayed vertical/horizontal slide-out states, and PLC-wait state using named object fields and functional entries. Preserve the backward `dbf` into the `$20F2F6` PLC destination word and retain the USA snapshot's historical `$203A72` draw and `$203B1E` delete targets as literals because they no longer identify current semantic entries. Expose the first two instructions of the following Results dispatcher at `$20F420` as the exact boundary for the next milestone. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The first USA comparison revealed that the replaced raw line also held the following dispatcher's six-byte prefix, shortening the image and moving `Padding1`; restoring those two instructions recovered the fixed layout. The fresh USA listing confirms the title-card copy at `$20F326`, loop target at `$20F2F6`, PLC wait at `$20F40A`, and Results prefix at `$20F420`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M117 | Complete | Recover the USA orphaned Results dispatcher and delayed graphics-load state at `$20F420-$20F44B`. Express its routine-byte indexed five-entry table with the initialization, PLC-wait, movement, bonus-tally, and next-level roles; keep offsets to later raw states numeric until their bodies receive exact-boundary labels. Replace the initialization bytes with named Results timer and routine fields plus the semantic `LoadPLC` service, and expose the PLC-wait state at `$20F44C` as the next milestone boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the new public labels are functional. The five table offsets `$000A`, `$001E`, `$00EA`, `$0124`, and `$01D8` establish state starts at `$20F438`, `$20F44C`, `$20F518`, `$20F552`, and `$20F606`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M118 | Complete | Recover the complete USA Results PLC-wait and three-object initialization state at `$20F44C-$20F517`. Name the PLC-empty, Stardust Speedway Act 3, player/camera threshold, object loop, Good/Bad Future mapping selection, and act-number-frame paths. Apply the eight-byte initialization-record contract and Results destination-X/timer fields while preserving historical absolute references to the records, four mapping tables, and allocator until those retained units are recovered. Document the USA snapshot's 480-frame display timer, distinct from the JE copy's 360 frames, and expose movement at `$20F518` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. The first USA comparison found two `$0168` values where the historical bytes require `$01E0`, proving the regional timer difference; correcting both restored byte identity. The fresh USA listing confirms initialization records at `$20F6E0`, mapping tables at `$20F6F8/$20F702/$20F70C/$20F716`, allocator `$207AFC`, and movement at `$20F518`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M119 | Complete | Recover the complete USA Results movement state at `$20F518-$20F551`. Name its timer countdown, signed eight-pixel horizontal convergence, destination arrival, map-frame gate, routine advance, and draw/return paths using the Results timer and destination-X fields. Preserve the USA snapshot's 472-frame draw threshold and historical `$203A72` draw target, both distinct from the JE copy's 352-frame threshold and `$203A6E` target, and expose the bonus-tally state at `$20F552` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the state uses functional labels. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M120 | Complete | Recover the complete USA Results bonus-tally state at `$20F552-$20F605`. Name the time/ring-bonus selection, hundred-point decrements, ten-point score increments, completion delay, optional warp sound, alternating tally sound, score addition, draw, and routine-advance paths. Apply the existing Results timer, bonus counters, HUD-update flag, special-stage flag, sound constants, and FM queue symbols while preserving the USA snapshot's historical `$202282`, `$20221A`, `$202236`, `$20A8F8`, and `$203A72` service operands. Expose the next-level state at `$20F606` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the state uses functional labels. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M121 | Complete | Recover the final USA Results executable state at `$20F606-$20F6DF`. Name and document its level-restart reset, Time Attack PLC flag handling, act/time-zone progression, intentionally redundant Act 3 rollover writes, transition-service calls, and Good Future completion bookkeeping. Apply established RAM and flag symbols while preserving the historical USA `$2078EA`, `$205C26`, and `$203A72` service operands, and expose the three eight-byte initialization records at `$20F6E0` as the next exact data boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the state uses functional labels. The complete USA orphaned Results dispatcher/state machine is now expressed as instructions across `$20F420-$20F6DF`, with no unexplained `dc.*` executable emission. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M122 | Complete | Structure the USA Results initialization and mapping-index data at `$20F6E0-$20F71F`. Express three eight-byte initial-Y/initial-X/destination-X/packed-frame records and four five-word Bad, Bad SSZ3, Good, and Good SSZ3 mapping tables. Document each offset position as header, standard or SSZ3 score panel, and Act 1-3 number frames; keep numeric offsets until the target frame bodies receive stable labels. Expose the first Bad header frame at `$20F720` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. The tables begin at `$20F6F8`, `$20F702`, `$20F70C`, and `$20F716`, matching the historical absolute pointers used by the recovered initializer. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M123 | Complete | Structure all four USA Bad-Future Results mapping frames at `$20F720-$20F7E5`: the eight-piece header and three ten-piece act-number variants. Document the count-prefixed five-byte signed-Y/size/tile/signed-X schema and explicit even alignment. Replace the Bad and Bad-SSZ3 tables' numeric header and act offsets with label-relative expressions while retaining their two score-panel offsets until those frames are recovered. Expose the Good-Future header at `$20F7E6` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every frame label is functional. The recovered frame starts at `$20F720`, `$20F74A`, `$20F77E`, and `$20F7B2` agree with all eight table references. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M124 | Complete | Structure all four USA Good-Future Results mapping frames at `$20F7E6-$20F8D5`: the fourteen-piece header and three eleven-piece act-number variants. Apply the established count-prefixed five-byte signed-Y/size/tile/signed-X schema and explicit even alignment. Replace the Good and Good-SSZ3 tables' numeric header and act offsets with label-relative expressions while retaining their two score-panel offsets until those frames are recovered. Expose the standard score frame at `$20F8D6` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every frame label is functional. The recovered frame starts at `$20F7E6`, `$20F82E`, `$20F866`, and `$20F89E` agree with all eight table references. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M125 | Complete | Complete the USA Results mapping family by structuring the standard and SSZ3 score frames at `$20F8D6-$20F965` as two fourteen-piece records under the established count-prefixed five-byte signed-Y/size/tile/signed-X schema with explicit even alignment. Replace the final numeric offsets in the Good and Good-SSZ3 mapping tables with label-relative expressions; the Bad tables already reference the same shared frames. Expose the following retained title-card placement unit at `$20F966` as the next exact boundary. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because both new frame labels are functional. The standard frame starts at `$20F8D6`, the SSZ3 variant at `$20F91E`, and both end cleanly at `$20F966`; their only payload difference is the final two score-glyph pieces. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M126 | Complete | Recover the complete USA retained title-card unit at `$20F966-$20FABF`. Structure its eight placement records as initial Y/X, destination X, and packed mapping-frame/slide-delay words; document the three placements that reuse the zone-name frame and the act-indexed selection of frames 4 through 6. Replace nine unexplained mapping offsets with label-relative backdrop, headline, zone-name, subtitle, three act-number, zone-label, and zone-number frames; express each frame as a count followed by five-byte sprite pieces and explicit alignment. Classify the final six bytes as the USA snapshot's orphaned absolute-jump trampoline to `$2064DE`, and expose the following stage descriptor at `$20FAC0`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. Inclusion tracing confirms only USA `R32A__.MMD` selects this provider through `r3/data_2a.asm`; comparison with the independently recovered Japan/Europe copy establishes the shared record semantics while preserving the USA-specific shifted addresses. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M127 | Complete | Recover the complete orphaned Collision Chaos Act 1 Present stage descriptor and graphics-loading graph retained at `$20FAC0-$20FCE5` in USA R32A. Express the three historical stage-resource pointers, layout flags, and palette IDs by their real widths. Replace the nineteen raw list offsets with label-relative Stage, Standard, four section, four update, intro, Tentou, Results, Signpost, and repeated fallback entries; structure all fourteen unique bodies as count-minus-one records followed by six-byte art-address/VRAM-destination pairs. Document that this is an unreachable historical snapshot whose embedded absolute art addresses cannot safely be rebound to the live graph, preserving the USA-specific `$0020DCD0` Signpost pointer, and expose the truncated `DEMO11A` PLC slice at `$20FCE6`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new name is functional. Inclusion tracing confirms only USA `R32A__.MMD` selects the provider; comparison with the independently recovered Japan/Europe snapshot proves the graph's record semantics and its 550-byte boundary. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M128 | Complete | Classify `$20FCE6-$20FDDB` in USA R32A as a retained `DEMO11A` Palmtree Panic Act 1 Present graphics-list slice. Express the truncated Cam 4 Full tail as the surviving Animals VRAM destination after its omitted art pointer, followed by five complete art/VRAM records. Structure the Cam 5 Full, Cam 1-5 Incremental, Results, and Signpost bodies as eight complete count-minus-one PLCs, retaining historical absolute pointers and the USA snapshot's distinct `$0020DB3C` big-ring-flash address. Expose the unrelated retained unit beginning at `$20FDDC`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because every new label is functional. Comparison with the recovered JE snapshot and the original `DEMO11A` data graph proves the PLC roles, six-byte record contract, exact 246-byte boundary, and the regional pointer difference. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M129 | Complete | Recover the USA retained animated-PLC helper family at `$20FDDC-$20FEBD`. Classify the leading `$FE60` as a tail word from a preceding historical unit, then express the VDP/DMA setup wrapper and its animated and static record-update helpers as named 68000 instructions and functional local control flow. Document the a1/a2/a4 record ABI, `$FF1980` staging-buffer copy, pointer advancement, and 0-updated/1-waiting return contract. Structure the intervening animated metadata as frame count, unused byte, four delay/index pairs, and four historical frame pointers, and expose the separate static metadata/data unit at `$20FEBE`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the recovered family uses only functional names. Cross-source comparison identifies byte-identical copies in the USA R32C/D padding streams and a semantic provider in the Palmtree Panic Act 1 Present legacy data; only USA `R32A__.MMD` consumes the edited copy. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M130 | Complete | Structure `$20FEBE-$20FED5` as two static animated-PLC metadata records containing reset delay, frame count, and three/two historical frame pointers. Recover the complete orphaned stage descriptor and graphics graph at `$20FED6-$20FFE7`: typed stage-art/block/chunk and flag/palette fields, a nineteen-entry label-relative Stage/Standard/Section/Results/Signpost index, and four unique count-minus-one PLC bodies with six-byte art-pointer/VRAM-destination records. Explicitly leave the source zone/variant unidentified because the retained bytes and duplicate consumers do not prove it, while preserving all historical absolute pointers. Expose `$20FFE8-$20FFFF` as a separate truncated PLC fragment. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because all new labels are functional. Cross-source comparison confirms the same retained word stream in the USA R32C/D padding providers, while exact offset analysis proves the metadata boundaries, 19-entry selection pattern, four complete PLC bodies, and the clean graph end at `$20FFE8`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M131 | Complete | Finish the USA R32A retained pre-chunk block by classifying `$20FFE8-$20FFFB` as the tail of a historical PLC: the surviving destination of an omitted record pointer followed by three complete six-byte art-pointer/VRAM-destination records. Structure `$20FFFC-$20FFFF` as the beginning of a following one-record PLC whose first art pointer is truncated after its `$0023` high word by the fixed `$210000` chunk boundary. Preserve literal pointers because longer duplicate slices establish the record order but not the source list or asset identities. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because both fragment labels are functional. Cross-source comparison finds the complete sequence in the USA legacy animation/data tails and multiple bounded later-level slices, proving both PLC contracts and the exact truncation. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. The complete `$20F2D6-$20FFFF` USA R32A retained pre-chunk block is now classified by exact range, matching the already completed Japan/Europe provider. |
| M132 | Complete | Begin semantic recovery of the 10,752-byte R32A post-chunk provider historically named `R32A Padding 2`. Prove that its first `$CB6` bytes are nine complete canonical Nemesis streams and replace their raw byte declarations with tracked `incbin` assets in exact historical order: metal platform, four-piece HV spikes, block, retract block, one-way barrier, fire shooter, rotating platform, KamaKama, and two-piece HV spikes. Correct the provider header to describe retained data rather than presumed alignment, while leaving the following unit at relative `$0CB6` explicitly unresolved. | Audit/check reports zero missing or case-mismatched includes, adds nine resolved active `incbin` references, and leaves the active address-derived-label total at 16,060 because the new aggregate label is functional. A read-only reconstruction of the emitted stream found exact whole-file matches at relative offsets `$0000`, `$0126`, `$0298`, `$037C`, `$0436`, `$04B2`, `$0632`, `$083E`, and `$0BDC`, ending cleanly at `$0CB6`. Inclusion tracing confirms the legacy USA `R32A__.MMD` is the active consumer; an unselected semantic Act 2 variant graph also names the provider. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M133 | Complete | Structure the complete normal KamaKama mapping family at relative `$0CB6-$0E65` in the R32A post-chunk provider. Replace 432 raw retained bytes with the canonical eight-frame `sprites/r3/kama_kama_1.asm` source, whose offset table and Walk A-D, Throw, Watch A/B, and Recover roles document the count-prefixed five-byte sprite-piece records. Preserve the copy's terminal alignment byte explicitly, and expose the next mapping-offset table at relative `$0E66`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the aggregate mapping label is functional. Exact byte analysis proves that the shared semantic include emits 431 bytes and that this historical copy retains one additional terminal alignment byte; an initial Japan comparison caught the missing byte through the shifted following data and absolute pointers. After restoring it explicitly, fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms the active consumer is legacy `R32A__.MMD`, while the unselected semantic Act 2 variant graph names the same provider. |
| M134 | Complete | Structure the complete alternate KamaKama mapping family at relative `$0E66-$0FC1` in the R32A post-chunk provider. Replace 348 raw retained bytes with the canonical eight-frame `sprites/r3/kama_kama_2.asm` source, preserving the normal family's Walk A-D, Throw, Watch A/B, and Recover roles and count-prefixed five-byte sprite-piece schema while selecting the alternate tile pieces. Expose the following retained unit at relative `$0FC2`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the aggregate mapping label is functional. Exact source and byte-boundary analysis proves that the sixteen-byte offset table plus eight aligned frame records end cleanly at `$0FC2`. Inclusion tracing confirms that legacy `R32A__.MMD` is the active consumer and that the unselected semantic Act 2 variant graph names the same provider. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M135 | Complete | Classify relative `$0FC2-$10FF` in the R32A post-chunk provider as the final `$13E` bytes of the canonical shared collision-column height map. Replace the 318 mechanically emitted bytes with a bounded `incbin` slice from offset `$EC2` of `maps/collision_columns.bin`, and document the source as 256 collision profiles of sixteen bytes each. Expose the following retained unit at relative `$1100`. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, and leaves the active address-derived-label total at 16,060 because the fragment label is functional. Read-only exact matching proves that the retained bytes equal the canonical file's `$EC2-$0FFF` suffix and end on its 4,096-byte boundary. Inclusion tracing confirms that legacy `R32A__.MMD` is the active consumer and that the unselected semantic Act 2 variant graph names the same provider. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M136 | Complete | Classify relative `$1100-$20FF` in the R32A post-chunk provider as the complete canonical shared row-oriented collision height map. Replace 4,096 mechanically emitted bytes with `maps/collision_rows.bin`, documenting its 256 sixteen-byte profiles and their block-collision metadata indexing. Expose the following retained unit at relative `$2100`. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, removes 745 raw declaration lines, and leaves the active address-derived-label total at 16,060 because the map label is functional. Read-only exact matching proves whole-file equality with the canonical 4,096-byte asset and a clean boundary at `$2100`. Inclusion tracing confirms that legacy `R32A__.MMD` is the active consumer and that the unselected semantic Act 2 variant graph names the same provider. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M137 | Complete | Classify relative `$2100-$241F` in the R32A post-chunk provider as a complete retained copy of the 800-byte Collision Chaos Act 1 Past block collision-index map. Replace the raw declarations with `maps/r31b/collision.bin`, document that the copy is independent of its live Act 1 consumer, and preserve the following unit's leading zero where the historical declaration run crossed the exact `$2420` boundary. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, removes 89 raw declaration lines, and leaves the active address-derived-label total at 16,060 because the retained-map label is functional. Whole-file matching proves exact equality with the canonical Act 1 Past asset and separates the next unit without changing its first byte. Inclusion tracing confirms that legacy `R32A__.MMD` is the active consumer and that the unselected semantic Act 2 variant graph names the same provider; `R31B__.MMD` independently consumes the canonical map through `r3/data_1b.asm`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M138 | Complete | Structure the 72-byte Collision Chaos Act 1 Past layout pointer table at relative `$2420-$2467` in the R32A post-chunk provider. Express its three identical section selections as twelve big-endian word offsets repeated three times, document the foreground/background/null triplet contract, and retain numeric offsets until the target layout bodies receive stable semantic labels. Expose the foreground layout at relative `$2468`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the table label is functional. Comparison with the independent live table in `r3/data_1b.asm` proves the twelve-slot roles, three repetitions, and exact first-target offset `$0048`. Inclusion tracing confirms that legacy `R32A__.MMD` is the active retained-copy consumer, the unselected semantic Act 2 variant graph names the same provider, and `R31B__.MMD` consumes the corresponding live layout graph. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M139 | Complete | Classify relative `$2468-$2589` in the R32A post-chunk provider as the complete 290-byte Collision Chaos Act 1 Past foreground layout. Replace the raw declarations with `maps/r31b/foreground.bin`, bind its boundary to the retained layout table's `$0048` target and following `$016A` background target, and preserve the first six background bytes where a declaration crossed the exact split. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, removes 54 raw declaration lines, and leaves the active address-derived-label total at 16,060 because the foreground label is functional. Whole-file matching proves exact equality with the canonical asset and its 290-byte extent. Inclusion tracing confirms that legacy `R32A__.MMD` is the active retained-copy consumer, the unselected semantic Act 2 variant graph names the provider, and `R31B__.MMD` independently consumes the canonical foreground through `r3/data_1b.asm`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M140 | Complete | Classify relative `$258A-$25A1` in the R32A post-chunk provider as the complete 24-byte Collision Chaos Act 1 Past background layout. Replace the raw declarations with `maps/r31b/background.bin`, convert all repeated foreground and background offsets in the retained table to label-relative expressions, and preserve the following four-byte null layout at relative `$25A2`. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, and leaves the active address-derived-label total at 16,060 because the background label is functional. Whole-file matching proves exact equality with the canonical asset and confirms the table's `$016A` boundary. Inclusion tracing confirms that legacy `R32A__.MMD` is the active retained-copy consumer, the unselected semantic Act 2 variant graph names the provider, and `R31B__.MMD` independently consumes the canonical background through `r3/data_1b.asm`. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M141 | Complete | Classify relative `$25A2-$25A5` in the R32A post-chunk provider as the complete shared four-byte null layout. Replace the zero declaration with `maps/empty.bin`, convert all repeated `$0182` null-layout offsets in the retained Act 1 Past table to label-relative expressions, and expose the following distinct retained layout at relative `$25A6`. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, and leaves the active address-derived-label total at 16,060 because the null-layout label is functional. Whole-file matching proves exact equality with the canonical asset and confirms the table boundary. Inclusion tracing confirms that legacy `R32A__.MMD` is the active retained-copy consumer, the unselected semantic Act 2 variant graph names the provider, and `R31B__.MMD` independently consumes the shared null layout in its live graph. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M142 | Complete | Classify relative `$25A6-$266D` in the R32A post-chunk provider as the complete retained 200-byte Green Hill Zone Act 2 foreground layout. Replace the raw declarations with `maps/ghz2_foreground.bin`, convert the repeated `$0186` table entries to label-relative expressions, preserve the following four-byte null target at `$266E`, and expose the next distinct layout at `$2672`. | Audit/check reports zero missing or case-mismatched includes, adds one resolved active `incbin`, removes 30 raw declaration lines, and leaves the active address-derived-label total at 16,060 because the GHZ2 label is functional. Whole-file matching proves exact equality with the canonical asset. The first Japan build identified an erroneously preserved extra zero after the boundary-crossing five-zero run; recounting it as the GHZ2 terminal byte plus exactly four null-layout bytes restored the original ISO extent. Fresh isolated displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms legacy `R32A__.MMD` as the active retained-copy consumer, the unselected semantic Act 2 variant graph as another provider reference, and `R31B__.MMD` as the independent live GHZ2 consumer. |
| M143 | Complete | Finish the table-addressed Collision Chaos Act 1 Past retained layout graph at relative `$266E-$279B`. Classify the `$024E` target as a GHZ2-associated four-byte null layout, `$0252` as the complete 290-byte `maps/ghz3_foreground.bin`, `$0374` as the shared null target, and `$0378` as the final null target; replace all four bodies with canonical assets and convert every remaining numeric table offset to a label-relative expression. Expose the unrelated retained unit at relative `$279C`. | Audit/check reports zero missing or case-mismatched includes, adds four resolved active `incbin` references, removes 45 raw declaration lines, and leaves the active address-derived-label total at 16,060 because all new labels are functional. Exact matching proves the GHZ3 whole-file extent and each four-byte null boundary. Inclusion tracing confirms legacy `R32A__.MMD` as the active retained-copy consumer, the unselected semantic Act 2 variant graph as another provider reference, and `R31B__.MMD` as the independent live layout-graph consumer. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. |
| M144 | Complete | Structure the 33-byte truncated legacy demo sprite-mapping frame at relative `$279C-$27BC` in the R32A post-chunk provider. Express its declared eight-piece count, six complete five-byte Y/size/tile/X records, and the retained Y plus size/shape bytes of an incomplete seventh piece; preserve the final size byte as the boundary shared with the following mapping-offset table. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the fragment label is functional. Cross-source matching finds the identical fragment within shared legacy demo-mapping tails and numerous bounded level snapshots. The first Japan comparison caught an initial 32-byte boundary assumption through shifted later data and absolute pointers; accounting for both retained fields of piece seven restored byte identity. Fresh isolated displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms legacy `R32A__.MMD` as the active consumer and the unselected semantic Act 2 variant graph as another provider reference. |
| M145 | Complete | Structure the overlapping ten-entry legacy demo mapping-offset table at relative `$27BC-$27CF` in the R32A post-chunk provider. Prove that the truncated preceding frame's retained size/shape zero is simultaneously the table's first byte, resolving the apparent odd-address/little-endian interpretation; express the table as ten normal big-endian word offsets `$0014-$01F2` relative to the shared byte, and expose frame 0 at `$27D0`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the table label is functional. Offset monotonicity, ten in-range frame starts, and the exact first-frame boundary prove the overlapping interpretation; it also matches the corresponding shared legacy demo-mapping tails. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms legacy `R32A__.MMD` as the active consumer and the unselected semantic Act 2 variant graph as another provider reference. |
| M146 | Complete | Structure retained legacy demo mapping frame 0 at relative `$27D0-$27F9`. Express its eight standard five-byte sprite pieces as a two-column/four-row arrangement, preserve its explicit alignment byte, replace the table's `$0014` entry with a label-relative expression, and expose frame 1 at `$27FA`. Avoid assigning an object or pose identity unsupported by the retained bytes. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, eight complete records, and alignment account for the exact 42-byte extent. Cross-source matching continues to tie the family to shared legacy demo-mapping tails without proving a narrower identity. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms legacy `R32A__.MMD` as the active consumer and the unselected semantic Act 2 variant graph as another provider reference. |
| M147 | Complete | Structure retained legacy demo mapping frame 1 at relative `$27FA-$2827`. Express its nine standard five-byte sprite pieces, replace the table's `$003E` entry with a label-relative expression, and expose frame 2 at `$2828`. Describe only the visible six-piece central arrangement and three farther-left pieces, without assigning an unsupported object or pose identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, and nine complete records account for the exact 46-byte extent with no alignment byte. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains unchanged: legacy `R32A__.MMD` is the active consumer and the unselected semantic Act 2 variant graph is another provider reference. |
| M148 | Complete | Structure retained legacy demo mapping frame 2 at relative `$2828-$2855`. Express its nine standard five-byte sprite pieces, replace the table's `$006C` entry with a label-relative expression, and expose frame 3 at `$2856`. Document its record arrangement and changed tile words/X positions without inventing an unsupported object or pose identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, and nine complete records account for the exact 46-byte extent with no alignment byte. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains unchanged: legacy `R32A__.MMD` is the active consumer and the unselected semantic Act 2 variant graph is another provider reference. |
| M149 | Complete | Structure retained legacy demo mapping frame 3 at relative `$2856-$2883`. Express its nine standard five-byte sprite pieces, replace the table's `$009A` entry with a label-relative expression, and expose frame 4 at `$2884`. Document the family arrangement and changed tile words/X positions without assigning an identity unsupported by the retained bytes. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, and nine complete records account for the exact 46-byte extent with no alignment byte. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms the active chain `Act 2 Present.asm` → `R32A Legacy.asm` → `R3 Legacy Act 2 Shared.asm` → `r3/data_2a.asm`, while the unselected semantic Act 2 variant remains the other provider reference. |
| M150 | Complete | Structure retained legacy demo mapping frame 4 at relative `$2884-$28A7`. Express its seven standard five-byte sprite pieces, replace the table's `$00C8` entry with a label-relative expression, and expose frame 5 at `$28A8`. Describe the visible three-narrow/two-wide upper arrangement and two lower pieces without assigning an unsupported identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, and seven complete records account for the exact 36-byte extent with no alignment byte. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M151 | Complete | Structure retained legacy demo mapping frame 5 at relative `$28A8-$28C7`. Express its six standard five-byte sprite pieces and explicit alignment byte, replace the table's `$00EC` entry with a label-relative expression, and expose frame 6 at `$28C8`. Describe the two narrow left pieces, two wider upper pieces, and two lower pieces without assigning an unsupported identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, six complete records, and alignment account for the exact 32-byte extent. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M152 | Complete | Structure retained legacy demo mapping frame 6 at relative `$28C8-$2919`. Express its sixteen standard five-byte sprite pieces and explicit alignment byte, replace the table's `$010C` entry with a label-relative expression, and expose frame 7 at `$291A`. Document the composite as the six-piece arrangement used by frame 2, frame 4's seven pieces, and three farther-left pieces without assigning an unsupported identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, sixteen complete records, and alignment account for the exact 82-byte extent. Matching byte runs occur in `r3/data_1a.asm`, `data_1b.asm`, `data_1d.asm`, and other Collision Chaos padding snapshots without proving a narrower identity. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M153 | Complete | Structure retained legacy demo mapping frame 7 at relative `$291A-$2965`. Express its fifteen standard five-byte sprite pieces, replace the table's `$015E` entry with a label-relative expression, and expose frame 8 at `$2966`. Document the composite as the six-piece arrangement used by frame 3, six pieces used by frame 5, and three farther-left pieces without assigning an unsupported identity. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the frame label is functional. The table offset, count byte, and fifteen complete records account for the exact 76-byte extent with no alignment. Matching copies corroborate the boundary without proving a narrower identity. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M154 | Complete | Structure retained legacy demo mapping frame 8 at relative `$2966-$29A2` and separately classify the anomalous `$29A3-$29AD` suffix inside its table-bounded span. Express the declared twelve standard pieces, two additional uncounted five-byte piece records, and alignment; replace the table's `$01AA` entry with a label-relative expression and expose frame 9 at `$29AE`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because both new labels are functional. The declared count accounts for 61 bytes, while the next table offset leaves exactly two more valid records plus one alignment byte; identical retained copies reproduce this discrepancy, so it is documented rather than normalized. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M155 | Complete | Structure retained legacy demo mapping frame 9 at relative `$29AE-$29EA` and its anomalous `$29EB-$29F5` suffix. Express the declared twelve standard pieces, two additional uncounted five-byte piece records, and alignment; replace the table's `$01F2` entry with a label-relative expression. Separate the remaining `$29F6-$29FF` five-word terminal pattern and leave its role explicitly unresolved. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because all new labels are functional. The declared count accounts for 61 bytes and the following eleven bytes resolve as two complete records plus alignment. The remaining ten bytes form `$E000,0,$E000,0,$E000`, outside the frame/tail structure and therefore not claimed complete. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M156 | Complete | Classify the final retained `$29F6-$29FF` bytes in `R32A Padding 2` as two post-alignment uncounted five-byte sprite-piece records, completing exact semantic accounting of the provider. Replace the misleading five-word representation with explicit Y/size/tile/X records while preserving the absence of a count or table target. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the terminal-tail label is functional. The ten bytes divide exactly under the established mapping grammar, and identical tails in the R13 retained data and USA legacy R72 animation data corroborate the post-alignment anomaly without establishing a narrower object identity. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing remains the active `R32A__.MMD` legacy chain plus the unselected semantic Act 2 variant provider reference. |
| M157 | Complete | Begin semantic classification of `R32B Padding 1` by structuring its relative `$0000-$0017` truncated title-card zone-number mapping tail. Express the retained final three bytes of piece 2, four complete standard five-byte pieces, and alignment, and expose the following executable fragment at relative `$0018`. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the tail label is functional. Exact comparison with the already-structured R32A title-card `.ZoneNumber` mapping proves the omitted count/leading records, retained byte boundary, four complete records, and alignment. Fresh isolated displayless Steam Proton Experimental builds completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. Inclusion tracing confirms `Act 2 Past.asm` → `R32B Legacy.asm` → `R3 Legacy Act 2 B Shared.asm` → `r3/data_2b.asm` as the active `R32B__.MMD` chain, with the unselected semantic Act 2 variant as another provider reference. |
| M158 | Complete | Structure the relative `$0018-$00AB` orphaned animated-tile DMA updater and its 192-longword staging-copy helper in `R32B Padding 1`; expose the following data graph at `$00AC`. Preserve the updater's anomalous word branch into the first data byte and its two historical PC-indexed sources rather than inventing valid live targets. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the new labels are functional. The first build exposed asm68k's rejection of `$Fxxx.w` spellings; the second showed that forward symbolic branch/index displacements in this phased block emit zeros. Using sign-extended `$FFFFFxxx.w` operands and five explicit, semantically commented historical forward encodings restored exact bytes. Fresh isolated displayless Steam Proton Experimental builds then completed for Japan, USA, and Europe, and all 133 comparisons matched in each region. The affected output and provider references remain `R32B__.MMD` plus the unselected semantic variant. |
| M159 | Complete | Structure the four-longword animated-tile source table at relative `$00AC-$00BB` in `R32B Padding 1`, naming its frame-0 primary/secondary and frame-1 primary/secondary roles and exposing the retained stage descriptor at `$00BC`. Preserve the copied historical addresses rather than falsely binding them to current-layout symbols. | Audit/check reports zero missing or case-mismatched includes and leaves the active address-derived-label total at 16,060 because the table labels are functional. The four pointer values correspond to the named Present raw-tile assets in `r3/data_2a.asm`, and the preceding updater's two PC-indexed operands select the first two entries while its anomalous BPL targets the table base. `r3/data_2b.asm` selects this provider only for Japan/Europe and uses the separate USA legacy Padding1 for USA. Fresh isolated displayless Steam Proton Experimental builds completed for all three regions, and all 133 comparisons matched in each. |
| M160 | Complete | Structure the 16-byte R32B Act 2 Past stage descriptor at `$00BC-$00CB`; expose the PLC-offset table at `$00CC`. | Descriptor contract and live graph corroborate the fields; J/U/E 133/133 each; audit/hygiene clean. |
| M161 | Complete | Structure the nineteen-entry R32B PLC-offset table at `$00CC-$00F1`; expose its first body at `$00F2`. | First offset proves table size; standard PLC slot contract corroborates roles; J/U/E 133/133 each; audit/hygiene clean. |
| M162 | Complete | Structure the R32B Stage PLC at `$00F2-$00FF`. | J/U/E 133/133 each; audit/hygiene clean. |
| M163 | Complete | Structure the R32B Standard PLC at `$0100-$015B`. | J/U/E 133/133 each; audit/hygiene clean. |
| M164 | Complete | Structure the R32B Section 0 PLC at `$015C-$01AB`. | J/U/E 133/133 each; audit/hygiene clean. |
| M165 | Complete | Structure the R32B Section 1 PLC at `$01AC-$01FB`. | J/U/E 133/133 each; audit/hygiene clean. |
| M166 | Complete | Structure the R32B Section 2 PLC at `$01FC-$0203`. | J/U/E 133/133 each; audit/hygiene clean. |
| M167 | Complete | Structure the R32B Section 3 PLC at `$0204-$020B`. | J/U/E 133/133 each; audit/hygiene clean. |
| M168 | Complete | Structure the R32B Results PLC at `$020C-$0213`. | J/U/E 133/133 each; audit/hygiene clean. |
| M169 | Complete | Structure the R32B Signpost PLC at `$0214-$0227`. | J/U/E 133/133 each; audit/hygiene clean. |
| M170 | Complete | Structure the retained Act 1 Present section-PLC tail at `$0228-$025B`. | J/U/E 133/133 each; audit/hygiene clean. |
| M171 | Complete | Structure the duplicate Results PLC at `$025C-$0263`. | J/U/E 133/133 each; audit/hygiene clean. |
| M172 | Complete | Structure the duplicate Signpost PLC at `$0264-$0277`. | J/U/E 133/133 each; audit/hygiene clean. |
| M173 | Complete | Structure the retained title-card executable fragment at `$0278-$0371`. | J/U/E 133/133 each after preserving historical PLC jump target; audit/hygiene clean. |
| M174 | Complete | Structure the retained Results dispatcher and delayed-load state at `$0372-$039D`. | J/U/E 133/133 each; audit/hygiene clean. |
| M175 | Complete | Structure the retained Results PLC-wait/object initialization state at `$039E-$0469`. | J/U/E 133/133 each; audit/hygiene clean. |
| M176 | Complete | Structure the retained Results movement state at `$046A-$04A3`. | J/U/E 133/133 each; audit/hygiene clean. |
| M177 | Complete | Structure the retained Results bonus-tally state at `$04A4-$0557`. | J/U/E 133/133 each; audit/hygiene clean. |
| M178 | Complete | Structure the retained Results next-level state at `$0558-$0631`. | J/U/E 133/133 each; audit/hygiene clean. |
| M179 | Complete | Structure the three retained Results initialization records at `$0632-$0649`. | J/U/E 133/133 each; audit/hygiene clean. |
| M180 | Complete | Structure the four retained Results mapping-offset tables at `$064A-$0671`. | J/U/E 133/133 each; audit/hygiene clean. |
| M181 | Complete | Structure the retained bad-future Results mapping frames at `$0672-$0737`. | J/U/E 133/133 each; audit/hygiene clean. |
| M182 | Complete | Structure the retained good-future Results mapping frames at `$0738-$0827`. | J/U/E 133/133 each; audit/hygiene clean. |
| M183 | Complete | Structure the retained standard and SSZ3 Results score frames at `$0828-$08B7`. | J/U/E 133/133 each; audit/hygiene clean. |
| M184 | Complete | Structure the eight retained title-card element records at `$08B8-$08F7`. | J/U/E 133/133 each; audit/hygiene clean. |
| M185 | Complete | Structure the retained title-card mapping-offset table at `$08F8-$0909`. | J/U/E 133/133 each; audit/hygiene clean. |
| M186 | Complete | Structure the retained title-card backdrop mapping frame at `$090A-$0929`. | J/U/E 133/133 each; audit/hygiene clean. |
| M187 | Complete | Structure the retained title-card headline and zone-name frames at `$092A-$0941`. | J/U/E 133/133 each; audit/hygiene clean. |
| M188 | Complete | Structure the retained title-card subtitle frame at `$0942-$095B`. | J/U/E 133/133 each; audit/hygiene clean. |
| M189 | Complete | Structure the retained title-card Act 1–3 mapping frames at `$095C-$09C7`. | J/U/E 133/133 each; audit/hygiene clean. |
| M190 | Complete | Structure the retained title-card zone-label and zone-number mapping frames at `$09C8-$0A0B`. | J/U/E 133/133 each; audit/hygiene clean. |
| M191 | Complete | Structure the retained title-card trampoline and stage descriptor at `$0A0C-$0A21`. | J/U/E 133/133 each; audit/hygiene clean. |
| M192 | Complete | Structure the retained nineteen-entry stage/PLC offset table at `$0A22-$0A47`. | J/U/E 133/133 each; audit/hygiene clean. |
| M193 | Complete | Structure the retained Stage PLC at `$0A48-$0A55`. | J/U/E 133/133 each; audit/hygiene clean. |
| M194 | Complete | Structure the retained Standard PLC at `$0A56-$0AB1`. | J/U/E 133/133 each; audit/hygiene clean. |

Add a row whenever a milestone is selected. Mark it complete only in the same
commit that contains the finished, validated work. The Git history and remote
branch are the authoritative commit record, so the table does not need to
embed its own commit hash.

## Roadmap

The roadmap is ordered by dependency and reviewability. Split any item further
when it cannot be understood, validated, committed, and pushed as one coherent
change.

### Track A: Establish a measurable baseline

1. Create `docs/disassembly-status.md` and the audit script described above.
2. Resolve false positives caused by conditional includes, generated includes,
   editor-only assembly data, and case-insensitive Windows include behavior.
3. Manually classify every active non-BRAM component as structured, hybrid, or
   legacy and identify its largest unresolved code/data ranges.

### Track B: Converge level source graphs

Migrate the existing parallel and hybrid graphs in this order:

1. Collision Chaos Act 1 (`R31A-D`)
2. Collision Chaos Act 2 (`R32A-D`)
3. Tidal Tempest Act 1 (`R41A-D`)
4. Tidal Tempest Acts 2 and 3 (`R42A-D`, `R43C-D`, and `DEMO43C`)
5. Quartz Quadrant (`R51A-D`, `R52A-D`, and `R53C-D`)
6. Stardust Speedway (`R71A-D`, `R72A-D`, and `R73C-D`)
7. Metallic Madness (`R81A-D`, `R82A-D`, `R83C-D`, and `DEMO82A`)

Treat each act family or smaller shared source cluster as a separate milestone.
Do not delete an alternative graph until all its consumers are accounted for
and the replacement matches every affected regional output.

### Track C: Recover active executable semantics

Within the active graphs:

1. Recover shared `common/` engine routines because they affect the most
   consumers.
2. Recover shared `objects/` routines in bounded object families.
3. Recover zone-specific `rN/` scroll, event, collision, object, boss, and
   stage-loading code.
4. Recover the standalone high-debt programs in independently verifiable
   address ranges:
   - Time Attack main CPU program;
   - ending FMV main CPU program; and
   - Thank You main CPU program.

For a large program, one semantic routine family is one milestone. Do not wait
for the entire program to be understood before committing and pushing useful,
verified progress.

### Track D: Classify and restructure data

Process active files named `Padding`, `Packed`, `Retained`, `Pre-Chunk`, or
`Legacy` by exact output range:

- executable bytes become instructions and labels;
- pointer, state, animation, mapping, path, and object records become named
  tables or macros;
- known compressed streams become descriptively named assets with the correct
  format extension;
- genuine padding becomes an alignment or fill directive with an explanation;
  and
- unresolved material remains explicitly recorded in the status matrix rather
  than being described as complete.

Then identify and rename the address-named `.bin` assets. Document a schema,
viewer, editor, extraction command, or round-trip process for each asset class.

### Track E: Make the documentation navigable

Create focused documentation as the corresponding knowledge becomes stable:

- `docs/architecture.md`
- `docs/build-graph.md`
- `docs/regional-variants.md`
- `docs/data-formats/`
- `docs/history/`
- a substantive `ida/README.md`

Reduce the main README to the project boundary, current headline status, build
instructions, and links to these documents. Preserve the existing milestone
history under `docs/history/` rather than discarding it.

### Track F: Enforce the standard

Add non-BRAM static checks for:

- new address-derived labels outside a reviewed allow-list;
- new address-named assets;
- unclassified large `dc.*` spans;
- include-path case mismatches;
- missing `include` or `incbin` targets;
- unexplained unreachable duplicate source;
- meaningful data named only as padding; and
- generated or proprietary files entering version control.

Where possible, add a component-only build mode that does not require the
externally supplied FMV streams or ISO text metadata. Public automation can run
static and source-only checks; protected local validation can perform the
regional byte comparisons and CHD data-track comparisons.

## Goal for a New Agent Instance

Use the following as the goal for a fresh instance:

> Execute the non-BRAM semantic disassembly remediation described in
> `DISASSEMBLY-REMEDIATION-PLAN.md`. Begin by reading `AGENTS.md`, the plan, the
> current progress ledger, `docs/disassembly-status.md` if it exists, and the
> latest relevant commits. Ignore Backup RAM work. Work autonomously on one
> bounded milestone at a time, preserving byte-exact Japan/USA/Europe output.
> For every milestone, trace the real entry points and shared consumers,
> implement semantic naming/structure/documentation or data classification,
> run the required narrow and regional validation, update the status matrix and
> milestone ledger, inspect repository hygiene, commit with an imperative
> subject, push immediately to the current upstream branch, and verify the
> remote commit before beginning the next milestone. Do not treat unexplained
> `dc.*` emission as semantic completion, but allow binary assets when their
> format and role are documented. Continue until the plan's completion criteria
> are met or a genuine external prerequisite blocks further progress.
